“Desenvolver uma solução baseada em Inteligência Artificial para auxiliar na identificação e verificação de desinformação relacionada à saúde, apresentando evidências e fontes confiáveis sem substituir o pensamento crítico do usuário.”

Fazer o projeto só com ferramentas gratuitas. Arquitetura 100% gratuita assim:

**Usuário → Streamlit → Python → busca de evidências → IA local → classificação/explicação → fontes**

| Parte | Ferramenta gratuita |
| --- | --- |
| Linguagem | Python |
| Interface/site | Streamlit |
| Manipulação de dados | Pandas |
| Machine Learning | Scikit-learn |
| IA generativa | Ollama + modelo open-source |
| Embeddings | Sentence Transformers |
| Banco vetorial/RAG | FAISS |
| Banco tradicional | SQLite |
| APIs/backend, se necessário | FastAPI |
| Código/versionamento | Git + GitHub |
| Experimentos | MLflow |
| Desenvolvimento | VS Code/Jupyter |
| Protótipo visual | Figma gratuito |

O Streamlit, por exemplo, é open source e permite construir a interface diretamente em Python. A própria documentação mostra como criar interfaces de chatbot e aplicações de IA. (Streamlit Docs)

### Como ficaria seu projeto

Imagine que alguém recebe no WhatsApp:

> **"Tomar determinada substância cura diabetes."**
> 

A pessoa cola essa informação no seu sistema.

Seu programa poderia executar:

**1. Identificar a alegação**

→ "Substância X cura diabetes."

**2. Procurar evidências**

O sistema consulta uma base que vocês montaram com conteúdos provenientes de fontes confiáveis, como órgãos de saúde e literatura científica.

**3. Recuperar as evidências mais relacionadas**

Aqui você pode implementar **RAG** gratuitamente com embeddings locais + FAISS.

**4. IA analisa as evidências**

Você não precisa necessariamente da API da OpenAI. Pode rodar um modelo open-source **localmente pelo Ollama**.

**5. O sistema apresenta algo como:**

> **🟠 Contradita pelas evidências**
> 
> 
> Foram encontradas evidências que contradizem essa alegação.
> 
> **Evidências encontradas**
> 
> Ministério da Saúde — [...]
> 
> Fiocruz — [...]
> 
> Artigo científico — [...]
> 
> **Por que merece atenção?**
> 
> A publicação afirma existir uma "cura", mas as evidências consultadas não sustentam essa conclusão.
> 
> **Analise as fontes abaixo antes de compartilhar.**
> 

Isso está muito alinhado ao documento de vocês, que propõe justamente categorias como **Sustentada, Parcialmente sustentada, Enganosa, Contradita pelas evidências e Não foi possível verificar**.

### E tem uma vantagem importante

Você **não precisa treinar uma IA gigantesca do zero**.

Isso tornaria o projeto caro e desnecessariamente complicado.

Para um MVP, eu seguiria:

**LLM local + RAG + base confiável de evidências.**

O sistema recupera documentos relevantes e utiliza o modelo para **analisar a alegação em relação às evidências recuperadas**, em vez de esperar que o modelo "saiba sozinho" se a notícia é verdadeira.

E isso combina muito bem com a própria investigação prevista no trabalho, que manda comparar **ML tradicional, modelo especializado, LLM e RAG + LLM**.

### Inclusive seu computador consegue ajudar bastante

Como você já tem uma **RTX 2060 6 GB + 32 GB RAM**, dá para experimentar modelos locais relativamente pequenos sem depender de API paga. Não significa que qualquer LLM vai rodar bem — modelos grandes serão pesados —, mas modelos menores/quantizados são suficientes para experimentar um MVP desse tipo.

Eu estruturaria o projeto em **6 etapas**:

1. **Dataset** — conseguir exemplos de desinformação relacionada à saúde.
2. **Fontes confiáveis** — montar a base de evidências.
3. **RAG** — recuperar evidências relacionadas à afirmação.
4. **LLM local** — analisar alegação + evidências.
5. **Streamlit** — criar a interface.
6. **MLflow/testes** — avaliar se o sistema está acertando e documentar os experimentos.

E nem a hospedagem precisa necessariamente custar. A documentação do Streamlit atualmente mantém uma opção de **Community Cloud gratuita** para compartilhar aplicações. (Streamlit Docs)

**Então sim: eu considero perfeitamente viável vocês entregarem um MVP funcional sem gastar dinheiro com software.**

O cuidado principal é não tentar fazer um "detector universal de fake news". **Restrinjam o MVP à saúde, texto e um conjunto controlado de fontes/evidências.** Isso deixa o projeto realizável e, tecnicamente, muito mais defensável.

# Entanção

Sim, **tem como fazer** — e para um MVP acadêmico dá para fazer praticamente tudo com ferramentas gratuitas.

A ideia da extensão seria esta: quando o usuário fizer uma busca no Google, a extensão lê os resultados exibidos na página e injeta, logo abaixo de cada resultado, uma análise como “Baixa confiabilidade”, “Evidências fortes”, “Fonte duvidosa” ou até uma porcentagem estimada. Extensões do Chrome conseguem injetar JavaScript em páginas e modificar o conteúdo exibido, então tecnicamente essa parte é viável. (Chrome for Developers)

Mas eu mudaria uma coisa importante no seu conceito: **não usaria “30% de chance de ser verdade” ou “80% de chance de ser verdade” como se fosse uma probabilidade objetiva**, porque isso é muito difícil de justificar cientificamente. Para o projeto de vocês, seria mais seguro e tecnicamente defensável mostrar algo como:

- **Evidências fortes a favor**
- **Parcialmente sustentada**
- **Evidências contraditórias**
- **Fonte com baixa confiabilidade**
- **Não foi possível verificar**
- **Confiança da análise: 82%**

Essa última porcentagem seria a **confiança do modelo na própria classificação**, e não “82% de chance da notícia ser verdadeira”. Isso combina muito melhor com o projeto do documento, que já propõe categorias como “Sustentada”, “Parcialmente sustentada”, “Enganosa”, “Contradita pelas evidências” e “Não foi possível verificar”.

A arquitetura poderia ser totalmente gratuita:

**Google → Extensão Chrome → lê título/snippet/link → envia para um backend local → RAG/IA analisa → devolve classificação → extensão mostra abaixo do resultado**

Você poderia usar **JavaScript + Manifest V3** na extensão, **Python + FastAPI** no backend, **FAISS** para busca vetorial, **Sentence Transformers** para embeddings, **Ollama + modelo open-source** para análise e **SQLite** para guardar dados. Tudo isso pode ser usado sem pagar licença.

O fluxo ficaria mais ou menos assim:

> Pesquisa: “chá X cura câncer”
> 

A página do Google mostra vários resultados.

A extensão captura cada título, trecho e URL.

Depois consulta sua IA.

E abaixo de um resultado apareceria algo como:

> 🟠 **Evidências contraditórias**
> 
> 
> Confiança da análise: 87%
> 
> A alegação não é sustentada pelas fontes científicas consultadas.
> 
> **Fontes verificadas:** Fiocruz, Ministério da Saúde, PubMed
> 
> “Ver evidências”
> 

Isso seria, inclusive, **um produto muito mais interessante do que simplesmente criar um site onde a pessoa cola uma notícia**, porque a ferramenta entra exatamente no momento em que o usuário está consumindo informação.

O principal desafio não é fazer a extensão. **A extensão é relativamente simples. O difícil é construir um sistema confiável de avaliação.** Você vai precisar definir como a IA decide a classificação, quais fontes são consideradas confiáveis, como recuperar evidências e o que fazer quando não existir evidência suficiente. Isso também está exatamente dentro das perguntas de pesquisa previstas no projeto.

Para não deixar o projeto grande demais, eu faria o **MVP somente para buscas de saúde e somente texto**. Nada de tentar analisar qualquer assunto da internet, imagem, vídeo, política etc. inicialmente.

Por exemplo:

**Versão 1 do projeto**

**Extensão Chrome de verificação de informações sobre saúde**

Ela analisaria:

**título + descrição + domínio do resultado + evidências recuperadas de uma base confiável.**

E poderia ter três componentes:

**1. Extensão Chrome**

Mostra a classificação diretamente no Google.

**2. Motor de IA**

Analisa as alegações.

**3. Base de evidências**

Ministério da Saúde, Anvisa, Fiocruz, PubMed e outras fontes previamente selecionadas.

E existe outro ponto interessante: vocês podem instalar a extensão localmente durante o desenvolvimento, sem precisar publicá-la na Chrome Web Store. A documentação oficial do Chrome oferece suporte e exemplos para desenvolvimento e carregamento de extensões. (Chrome for Developers)

Então, para a faculdade, vocês poderiam chegar na apresentação, abrir o Chrome, pesquisar:

> **“vacina da gripe causa gripe?”**
> 

e os resultados aparecerem já com as avaliações da IA embaixo.

Isso teria **muito mais impacto visual na apresentação** do que simplesmente mostrar um notebook executando um modelo.

E o mais importante: **sim, é um projeto que dá para construir.** Eu só evitaria vender a ideia como “IA que sabe a porcentagem de uma notícia ser verdadeira”. Eu apresentaria como:

> **“Uma extensão inteligente para Chrome que analisa resultados de busca relacionados à saúde, cruza as alegações com fontes confiáveis e apresenta ao usuário evidências e um nível de confiança antes que ele acesse ou compartilhe o conteúdo.”**
> 

Essa formulação é tecnicamente mais forte e está bem alinhada ao objetivo do trabalho.

Projeto IA Fake  News

----------------------------------------------------------------------------------------------------------
-

Perfeito. Vamos transformar isso em um **roteiro de desenvolvimento**, para você sempre saber em que etapa está, o que já terminou e qual é o próximo passo.

O projeto final será:

> **Uma extensão do Chrome que analisa resultados de pesquisas de saúde no Google e mostra, embaixo de cada resultado, uma classificação de confiabilidade baseada em evidências e fontes confiáveis.**

Vou dividir em **12 etapas**.

| Etapa | Objetivo                     | Resultado esperado                                           |
| ----- | ---------------------------- | ------------------------------------------------------------ |
| 1     | Definir o MVP                | Saber exatamente o que o sistema fará e o que ficará de fora |
| 2     | Criar extensão Chrome básica | Extensão instalada e funcionando                             |
| 3     | Ler resultados do Google     | Capturar título, descrição e URL                             |
| 4     | Inserir análise visual       | Mostrar um selo embaixo de cada resultado                    |
| 5     | Criar backend                | API em Python/FastAPI funcionando                            |
| 6     | Conectar extensão + backend  | Extensão envia resultados e recebe resposta                  |
| 7     | Criar base de evidências     | Ter documentos confiáveis de saúde                           |
| 8     | Criar busca semântica        | Encontrar evidências relacionadas à pesquisa                 |
| 9     | Integrar IA                  | IA analisa alegação + evidências                             |
| 10    | Criar classificação          | Definir resultado e nível de confiança                       |
| 11    | Testar e avaliar             | Medir erros, acertos e limitações                            |
| 12    | Preparar apresentação        | README, arquitetura, vídeo/demo e documentação               |

## ETAPA 1 — Definir exatamente o MVP

Antes de programar, vamos especificar o produto.

O **MVP 1.0** deve fazer somente isto:

1. usuário pesquisa alguma coisa no Google;
2. extensão detecta os resultados;
3. captura título, descrição e URL;
4. analisa somente conteúdos relacionados à saúde;
5. consulta nossa base de evidências;
6. apresenta uma classificação;
7. mostra as fontes/evidências utilizadas.

Exemplo:

> 🔴 **Contradita pelas evidências**
> Confiança da análise: 87%
> Encontramos evidências que contradizem essa afirmação.
> Fontes: Ministério da Saúde, Fiocruz e PubMed.

E vamos deixar **fora da versão inicial**:

* vídeos;
* imagens;
* Instagram;
* WhatsApp;
* política;
* notícias esportivas;
* qualquer idioma além de português;
* análise da internet inteira.

Isso evita o maior risco do projeto: tentar fazer coisa demais.

---

# ETAPA 2 — Extensão Chrome básica

Aqui **não existe IA ainda**.

Você vai estudar:

* HTML;
* CSS;
* JavaScript básico;
* `manifest.json`;
* Chrome Extensions;
* Content Scripts;
* DOM.

Primeiro objetivo:

Você pesquisa alguma coisa no Google e aparece:

> 🔎 HealthCheck IA — aguardando análise

embaixo de cada resultado.

Quando isso funcionar, considero a Etapa 2 concluída.

---

# ETAPA 3 — Capturar os resultados do Google

Agora a extensão precisa entender a página.

Ela deverá capturar algo assim:

```javascript
{
    titulo: "Vitamina C cura gripe?",
    descricao: "Veja o que dizem os estudos...",
    url: "https://exemplo.com/artigo"
}
```

Você vai estudar:

* DOM;
* `querySelector()`;
* `querySelectorAll()`;
* arrays;
* objetos JavaScript;
* manipulação de elementos HTML.

Meta:

```text
Resultado 1
Título: ...
URL: ...
Descrição: ...

Resultado 2
Título: ...
URL: ...
Descrição: ...
```

aparecendo no console.

---

# ETAPA 4 — Criar o componente visual

Agora vamos criar o visual que aparecerá no Google.

Inicialmente será falso/simulado:

```text
🟢 Evidências favoráveis

Confiança: 85%

Ver análise
```

ou:

```text
🔴 Evidências contraditórias

Confiança: 82%

Ver evidências
```

Aqui entra:

* CSS;
* componentes;
* badges;
* botões;
* expansão/recolhimento;
* acessibilidade básica.

Nesse momento a extensão já vai **parecer um produto real**, mesmo sem IA.

---

# ETAPA 5 — Backend Python

Agora criamos o servidor responsável pela inteligência.

Tecnologia:

**Python + FastAPI**

Você vai estudar:

* API;
* REST;
* HTTP;
* GET;
* POST;
* JSON;
* request;
* response;
* FastAPI;
* Pydantic;
* CORS.

Teremos algo como:

```text
POST /analisar
```

Entrada:

```json
{
  "titulo": "Vitamina C cura gripe?",
  "descricao": "..."
}
```

Resposta inicialmente simulada:

```json
{
  "classificacao": "enganosa",
  "confianca": 0.82
}
```

Essa etapa é importantíssima porque separa:

```text
EXTENSÃO
```

de:

```text
INTELIGÊNCIA DO SISTEMA
```

---

# ETAPA 6 — Conectar Chrome com Python

Aqui os dois projetos se encontram.

Fluxo:

```text
Google
 ↓
Extensão
 ↓
JavaScript
 ↓
HTTP POST
 ↓
FastAPI
 ↓
JSON
 ↓
Extensão
 ↓
Google
```

Vamos utilizar JavaScript:

```javascript
fetch(...)
```

A extensão manda a notícia para nosso backend.

E recebe:

```json
{
    "classificacao": "contradita",
    "confianca": 0.91
}
```

Então muda automaticamente o selo.

Quando isso acontecer, teremos uma arquitetura cliente-servidor funcionando.

---

# ETAPA 7 — Base de conhecimento em saúde

Aqui começa a parte científica.

Precisaremos construir uma base confiável.

Seu próprio projeto menciona referências como:

* Ministério da Saúde;
* Anvisa;
* Fiocruz;
* SUS;
* BVS;
* PubMed;
* universidades;
* sociedades médicas. 

Inicialmente eu não tentaria conectar tudo automaticamente.

Pegamos um conjunto pequeno de documentos confiáveis.

Algo como:

```text
data/

vacinas/
diabetes/
hipertensao/
cancer/
dengue/
medicamentos/
```

E colocamos documentos dentro.

---

# ETAPA 8 — Embeddings + busca semântica

Essa é uma etapa fundamental.

Imagine que sua base possui:

> “Não existem evidências científicas de que vitamina C cure infecções por influenza.”

E o usuário pesquisa:

> “Tomar vitamina C acaba com a gripe?”

As frases não são iguais.

Mas semanticamente estão relacionadas.

Para encontrar isso vamos estudar:

* NLP;
* embeddings;
* vetores;
* similaridade de cosseno;
* Sentence Transformers;
* chunking;
* busca vetorial.

A arquitetura será:

```text
Pesquisa
   ↓
Embedding
   ↓
Busca vetorial
   ↓
Documentos semelhantes
```

---

# ETAPA 9 — Construir o RAG

Depois juntamos:

**Retrieval + IA.**

O fluxo será:

```text
Alegação
 ↓
Busca semântica
 ↓
Encontrar 3-5 evidências
 ↓
LLM
 ↓
Análise
```

A IA receberá algo parecido com:

```text
ALEGAÇÃO:

"Vitamina C cura gripe."

EVIDÊNCIA 1:
...

EVIDÊNCIA 2:
...

EVIDÊNCIA 3:
...

Utilizando SOMENTE essas evidências,
avalie a alegação.
```

Isso reduz bastante o risco de a IA inventar informações.

---

# ETAPA 10 — Integrar o LLM

Para tentar manter tudo gratuito, podemos experimentar:

**Ollama + modelo open-source.**

A IA não precisa saber tudo.

Ela receberá as evidências fornecidas pelo RAG.

Estudaremos:

* LLM;
* contexto;
* prompt;
* system prompt;
* temperatura;
* hallucination;
* structured output;
* JSON Schema.

Queremos obrigar a IA a retornar algo assim:

```json
{
    "classificacao": "contradita",
    "confianca": 0.91,
    "explicacao": "As evidências encontradas...",
    "fontes": [
        "Fiocruz",
        "Ministério da Saúde"
    ]
}
```

---

# ETAPA 11 — Sistema de classificação

Vamos padronizar as possíveis respostas.

Eu sugiro usar as categorias já previstas pelo projeto:

```text
🟢 SUSTENTADA

🟡 PARCIALMENTE SUSTENTADA

🟠 ENGANOSA

🔴 CONTRADITA PELAS EVIDÊNCIAS

⚪ NÃO FOI POSSÍVEL VERIFICAR
```

Essas categorias estão presentes no planejamento original. 

Isso é melhor que:

> 25% verdadeira

porque uma porcentagem dessas pode parecer cientificamente precisa sem realmente ser.

Podemos ter:

> **Classificação:** contradita pelas evidências
> **Confiança do sistema:** 86%

---

# ETAPA 12 — Avaliação científica

Aqui você transforma um programa interessante em um **projeto acadêmico sério**.

Criaremos um dataset de teste.

Por exemplo:

```text
200 alegações
```

Cada uma já terá um resultado correto conhecido.

Depois:

```text
Sistema analisa
       ↓
comparamos
       ↓
resultado esperado
       X
resultado produzido
```

Vamos calcular:

* accuracy;
* precision;
* recall;
* F1-score;
* matriz de confusão.

Também podemos utilizar **MLflow** para registrar os experimentos.

Você vai conseguir dizer na apresentação:

> “Nosso modelo foi avaliado em 200 alegações verificadas e atingiu F1-score de X.”

Isso é muito mais forte do que apenas:

> “Olha, nossa IA funciona.”

---

# ETAPA 13 — Pensamento crítico

Essa parte precisa existir no produto.

Seu documento deixa claro que uma das questões centrais é evitar que a IA substitua a capacidade de julgamento da pessoa. 

Então não mostraremos apenas:

> ❌ FAKE

Mostraremos:

> 🔴 **Evidências contraditórias**
>
> Encontramos três fontes que apresentam evidências diferentes da afirmação.
>
> **Veja por quê**
>
> [...]
>
> **Consulte as fontes**
>
> [...]
>
> Antes de compartilhar, compare as evidências apresentadas.

A IA vira uma ferramenta de **apoio ao raciocínio**, não uma autoridade absoluta.

---

# ETAPA 14 — Segurança e limitações

Precisaremos estabelecer regras.

Por exemplo:

Se a evidência for insuficiente:

> ⚪ **Não foi possível verificar**

Nunca deixar a IA inventar uma resposta.

Também precisamos documentar:

* limitações;
* possíveis vieses;
* atualização da base;
* falsos positivos;
* falsos negativos;
* uso responsável.

O próprio planejamento prevê uma política de **abstenção da IA quando as evidências forem insuficientes ou contraditórias**. 

---

# ETAPA 15 — GitHub e documentação

Depois vamos profissionalizar.

Estrutura aproximada:

```text
healthcheck-ai/

extension/
backend/
ai/
data/
tests/
docs/
README.md
requirements.txt
.gitignore
```

E vamos trabalhar com commits organizados:

```text
feat: create chrome extension structure

feat: capture google search results

feat: create analysis api

feat: integrate extension with api

feat: implement semantic retrieval

feat: integrate local llm

test: add classification evaluation
```

---

# ETAPA 16 — Apresentação

No final teremos uma demonstração extremamente simples.

Você abre o Chrome.

Pesquisa:

```text
vacina da gripe causa gripe
```

Google mostra os resultados.

Alguns segundos depois:

```text
🔴 Evidências contraditórias
Confiança: 91%

3 evidências encontradas

[Ver evidências]
```

Clica.

Aparecem as fontes.

Depois você explica:

```text
Chrome Extension
       ↓
FastAPI
       ↓
RAG
       ↓
Base científica
       ↓
LLM
       ↓
Resposta explicável
```

E fecha apresentando as métricas dos testes.

---

## Nosso mapa oficial

Para você não se perder, vamos usar este acompanhamento:

```text
PROJETO — HEALTHCHECK IA

[ ] 01 — Definir MVP
[ ] 02 — Criar extensão Chrome
[ ] 03 — Capturar resultados Google
[ ] 04 — Criar interface dos selos
[ ] 05 — Criar FastAPI
[ ] 06 — Conectar extensão + API
[ ] 07 — Criar base de saúde
[ ] 08 — Implementar embeddings
[ ] 09 — Implementar busca vetorial
[ ] 10 — Implementar RAG
[ ] 11 — Integrar LLM local
[ ] 12 — Criar classificação
[ ] 13 — Mostrar evidências/fontes
[ ] 14 — Implementar pensamento crítico
[ ] 15 — Criar testes
[ ] 16 — Avaliar métricas
[ ] 17 — Integrar MLflow
[ ] 18 — Documentar limitações
[ ] 19 — Organizar GitHub
[ ] 20 — Preparar demonstração
[ ] 21 — Preparar apresentação
```

E tem uma regra que quero que a gente siga durante todo o desenvolvimento:

> **Não avançamos para a próxima camada até a anterior estar funcionando.**

Então nosso **primeiro objetivo real é muito pequeno**:

**criar uma extensão Chrome local que consiga alterar a página de resultados do Google e escrever “HealthCheck IA — aguardando análise” embaixo dos resultados.**

A partir daí, construímos tudo em cima dela.


----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

Perfeito. Vamos considerar a **ETAPA 1 oficialmente iniciada** e transformar isso em uma especificação simples do produto, para a gente não se perder depois.

### MVP 1.0 — HealthCheck IA

**Objetivo:** criar uma extensão para Google Chrome que analise resultados de pesquisas relacionadas à saúde e apresente ao usuário uma avaliação baseada em evidências confiáveis.

**Fluxo principal:**

```text
Usuário pesquisa no Google
        ↓
Extensão identifica os resultados
        ↓
Captura título + descrição + URL
        ↓
Verifica se o assunto é relacionado à saúde
        ↓
Busca evidências confiáveis
        ↓
IA compara a alegação com as evidências
        ↓
Classifica o conteúdo
        ↓
Mostra resultado + explicação + fontes
```

A classificação inicial pode seguir este padrão:

```text
🟢 Sustentada pelas evidências

🟡 Parcialmente sustentada

🟠 Enganosa

🔴 Contradita pelas evidências

⚪ Não foi possível verificar
```

E podemos ter separadamente:

```text
Confiança da análise: 87%
```

Esse número significa **confiança do sistema na classificação**, e não “87% de chance de a notícia ser verdadeira”.

### O que entra no MVP

O sistema vai trabalhar apenas com **texto em português relacionado à saúde**, inicialmente a partir dos resultados de pesquisas do Google. Ele analisará título, descrição e URL e utilizará uma base de evidências selecionada por nós.

As fontes poderão incluir, por exemplo, Ministério da Saúde, Anvisa, Fiocruz, BVS, PubMed e outras instituições que definirmos durante a construção da base.

### O que não entra agora

Para evitar aumentar demais o projeto, a primeira versão não terá análise de imagens, vídeos, áudio, Instagram, WhatsApp, política, esportes, outros idiomas ou análise automática de qualquer conteúdo da internet.

Também não vamos tentar construir uma IA própria do zero.

### Um exemplo real

Usuário pesquisa:

```text
chá de boldo cura diabetes
```

O Google apresenta:

```text
Chá de boldo pode acabar com diabetes...
site-exemplo.com
```

Nossa extensão poderia inserir logo abaixo:

```text
🔴 Contradita pelas evidências

Confiança da análise: 91%

As evidências encontradas não sustentam
a alegação de que o chá de boldo seja capaz
de curar diabetes.

3 evidências encontradas.

[Ver evidências]
```

Ao clicar em **Ver evidências**, poderiam aparecer as fontes utilizadas.

### Critérios para considerarmos o MVP concluído

A versão 1.0 estará pronta quando conseguirmos fazer uma demonstração completa:

```text
Pesquisa Google
      ↓
Resultado aparece
      ↓
Extensão captura
      ↓
Backend recebe
      ↓
Base encontra evidências
      ↓
IA analisa
      ↓
Extensão apresenta classificação
      ↓
Usuário consegue consultar as evidências
```

Não precisa ser perfeito, não precisa funcionar para toda doença e nem para toda notícia da internet. Precisa demonstrar que **a arquitetura e a ideia funcionam**.

Então nosso controle fica:

```text
PROJETO — HEALTHCHECK IA

[X] 01 — Definir MVP
[ ] 02 — Criar extensão Chrome
[ ] 03 — Capturar resultados Google
[ ] 04 — Criar interface dos selos
[ ] 05 — Criar FastAPI
[ ] 06 — Conectar extensão + API
[ ] 07 — Criar base de saúde
[ ] 08 — Implementar embeddings
[ ] 09 — Implementar busca semântica
[ ] 10 — Implementar RAG
[ ] 11 — Integrar LLM
[ ] 12 — Criar classificação
[ ] 13 — Mostrar evidências
[ ] 14 — Criar testes
[ ] 15 — Avaliar o sistema
[ ] 16 — MLflow
[ ] 17 — Documentação/GitHub
[ ] 18 — Apresentação
```

**ETAPA 1 concluída. ✅**

Agora podemos entrar na **ETAPA 2 — criar a primeira extensão Chrome do zero**. Nessa etapa ainda não vamos usar IA: o primeiro objetivo será simplesmente fazer o Chrome mostrar **“HealthCheck IA — aguardando análise”** embaixo dos resultados do Google.

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------


Certo. Então agora entramos oficialmente na **ETAPA 2**.

O objetivo aqui é bem simples: criar uma extensão local do Chrome que rode na página do Google e escreva, abaixo dos resultados:

> 🔎 HealthCheck IA — aguardando análise

Nesta etapa, **não entra IA, FastAPI, Python, banco de dados nem RAG**. Vamos provar apenas que a extensão consegue entrar na página do Google e modificar visualmente os resultados.

Você vai precisar criar uma pasta assim:

```text
healthcheck-extension/
│
├── manifest.json
├── content.js
└── styles.css
```

O `manifest.json` define a extensão e quais páginas ela pode acessar:

```json
{
  "manifest_version": 3,
  "name": "HealthCheck IA",
  "version": "1.0",
  "description": "Extensão para auxiliar na verificação de informações de saúde.",
  "permissions": [],
  "content_scripts": [
    {
      "matches": [
        "https://www.google.com/*"
      ],
      "js": [
        "content.js"
      ],
      "css": [
        "styles.css"
      ]
    }
  ]
}
```

No `content.js`, vamos procurar os resultados do Google e inserir nosso aviso:

```javascript
function adicionarHealthCheck() {
  const resultados = document.querySelectorAll("div.MjjYud");

  resultados.forEach((resultado) => {
    if (resultado.querySelector(".healthcheck-box")) {
      return;
    }

    const titulo = resultado.querySelector("h3");

    if (!titulo) {
      return;
    }

    const caixa = document.createElement("div");

    caixa.className = "healthcheck-box";
    caixa.innerHTML = `
      <strong>🔎 HealthCheck IA</strong>
      <span>Aguardando análise</span>
    `;

    resultado.appendChild(caixa);
  });
}

adicionarHealthCheck();

const observer = new MutationObserver(() => {
  adicionarHealthCheck();
});

observer.observe(document.body, {
  childList: true,
  subtree: true
});
```

E no `styles.css`:

```css
.healthcheck-box {
    margin-top: 8px;
    padding: 10px 14px;

    border: 1px solid #bcd7f5;
    border-radius: 8px;

    background-color: #f4f9ff;

    font-family: Arial, sans-serif;
    font-size: 14px;

    display: flex;
    align-items: center;
    gap: 10px;

    color: #1f4e79;
}

.healthcheck-box strong {
    font-weight: bold;
}

.healthcheck-box span {
    color: #666;
}
```

Depois de criar esses três arquivos, abra no Chrome:

```text
chrome://extensions
```

Ative:

```text
Modo do desenvolvedor
```

Clique em:

```text
Carregar sem compactação
```

E selecione a pasta:

```text
healthcheck-extension
```

Depois abra o Google e pesquise, por exemplo:

```text
vitamina C cura gripe
```

Se tudo funcionar, abaixo dos resultados deverá aparecer:

```text
🔎 HealthCheck IA   Aguardando análise
```

Se aparecer, podemos marcar:

```text
[X] ETAPA 2 — Extensão Chrome básica
```

O que você precisa entender nesta etapa é principalmente: **`manifest.json` diz ao Chrome quando executar a extensão; `content.js` entra na página e manipula o DOM; `styles.css` controla o visual; `MutationObserver` observa mudanças porque o Google pode adicionar resultados dinamicamente.**

Quando você criar esses três arquivos e testar, pode me mandar **print da tela ou qualquer erro que aparecer**. Aí eu vou corrigindo com você até a extensão funcionar.


--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

Perfeito. Agora vamos fazer a **ETAPA 3** de forma bem controlada.

O objetivo é fazer o `content.js` capturar, para cada resultado do Google:

```text
Título
URL
Descrição
```

e mostrar tudo no console.

Substitua o conteúdo atual do seu `content.js` por este:

```javascript
function capturarResultados() {
  const resultados = document.querySelectorAll("div.MjjYud");

  const dados = [];

  resultados.forEach((resultado, index) => {
    const tituloElemento = resultado.querySelector("h3");
    const linkElemento = tituloElemento?.closest("a");

    // Alguns resultados podem não ter descrição
    const descricaoElemento =
      resultado.querySelector(".VwiC3b") ||
      resultado.querySelector("[data-sncf]");

    if (!tituloElemento || !linkElemento) {
      return;
    }

    const item = {
      titulo: tituloElemento.innerText.trim(),
      url: linkElemento.href,
      descricao: descricaoElemento
        ? descricaoElemento.innerText.trim()
        : "Descrição não encontrada"
    };

    dados.push(item);

    console.log(`Resultado ${dados.length}`);
    console.log("Título:", item.titulo);
    console.log("URL:", item.url);
    console.log("Descrição:", item.descricao);
    console.log("-----------------------------");
  });

  console.log("Todos os resultados capturados:");
  console.log(dados);

  return dados;
}

capturarResultados();
```

Agora salve o arquivo.

Depois vá em:

```text
chrome://extensions
```

Encontre o **HealthCheck IA** e clique no botão de:

```text
↻ Recarregar
```

Depois volte ao Google e faça uma pesquisa, por exemplo:

```text
vitamina C cura gripe
```

Agora abra o console:

```text
F12
```

e vá na aba:

```text
Console
```

Você deve ver algo parecido com:

```text
Resultado 1

Título: Vitamina C ajuda no tratamento da gripe?

URL: https://exemplo.com/artigo

Descrição: Saiba o que estudos científicos dizem...

-----------------------------

Resultado 2

Título: Vitamina C previne resfriados?

URL: https://outrosite.com/artigo

Descrição: Pesquisas analisam os efeitos...

-----------------------------
```

E no final:

```javascript
[
  {
    titulo: "...",
    url: "...",
    descricao: "..."
  },
  {
    titulo: "...",
    url: "...",
    descricao: "..."
  }
]
```

O ponto mais importante dessa etapa é você entender o que cada linha faz.

`document.querySelectorAll("div.MjjYud")` procura **vários elementos** da página do Google que representam resultados.

`resultado.querySelector("h3")` procura o título dentro daquele resultado.

`tituloElemento?.closest("a")` sobe na estrutura HTML até encontrar o link `<a>` associado ao título.

Então conseguimos:

```javascript
tituloElemento.innerText
```

para pegar o texto do título.

E:

```javascript
linkElemento.href
```

para pegar a URL.

Já a descrição é procurada aqui:

```javascript
resultado.querySelector(".VwiC3b")
```

Esse nome vem da estrutura atual do Google e pode mudar no futuro. Isso é normal em scraping/manipulação de DOM de páginas que não controlamos.

Depois criamos um **objeto JavaScript**:

```javascript
const item = {
  titulo: "...",
  url: "...",
  descricao: "..."
};
```

E colocamos esse objeto dentro de um array:

```javascript
dados.push(item);
```

Então conceitualmente temos:

```text
Google
   ↓
querySelectorAll()
   ↓
resultado individual
   ↓
querySelector()
   ↓
título
URL
descrição
   ↓
objeto JavaScript
   ↓
array
```

### Um detalhe importante

Nesta etapa, **não coloque ainda IA nem classificação**.

A única pergunta que queremos responder agora é:

> "Minha extensão consegue transformar os resultados visuais do Google em dados estruturados?"

Se a resposta for sim, a Etapa 3 está praticamente concluída.

Quando testar, me mande o que apareceu no Console. Se algum campo vier vazio, URL errada ou descrição como `"Descrição não encontrada"`, eu ajusto o seletor com você.


--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------


Sim! 🎉 **A ETAPA 3 funcionou.** O resultado do seu console mostra exatamente o que queríamos obter.

Você conseguiu capturar **10 resultados do Google** e transformar cada um em um objeto JavaScript com:

```javascript
{
    titulo: "...",
    url: "...",
    descricao: "..."
}
```

Por exemplo, seu Resultado 1 foi capturado corretamente:

```text
Resultado 1

Título:
Vitamina C previne gripe e resfriados?

URL:
https://drauziovarella.uol.com.br/...

Descrição:
A vitamina C não reduz de forma significativa...
```

E, mais importante ainda, no final você tem:

```text
Todos os resultados capturados:

Array(10)
```

Ou seja, conceitualmente sua extensão já fez:

```text
Página do Google
      ↓
Encontra os resultados no DOM
      ↓
querySelectorAll()
      ↓
Percorre cada resultado
      ↓
Extrai:
Título
URL
Descrição
      ↓
Cria objetos JavaScript
      ↓
dados.push(item)
      ↓
Array com 10 objetos
```

### Um pequeno detalhe que NÃO é erro

No Resultado 2 apareceu:

```text
Descrição: Descrição não encontrada
```

Isso é esperado. O Google não necessariamente usa exatamente a mesma estrutura HTML para todos os tipos de resultados.

E nosso código já foi preparado para lidar com isso:

```javascript
descricao: descricaoElemento
    ? descricaoElemento.innerText.trim()
    : "Descrição não encontrada"
```

Portanto, ele não quebrou. Pelo contrário: **tratou corretamente a ausência do elemento.**

Esse tipo de tratamento vai ser importante mais adiante porque não podemos presumir que todo resultado do Google terá os mesmos campos.

## 🏆 Status do projeto

```text
HEALTHCHECK IA

[X] ETAPA 1 — Definir o MVP
[X] ETAPA 2 — Extensão Chrome básica
[X] ETAPA 3 — Capturar resultados do Google ✅

[ ] ETAPA 4 — Criar interface dos selos
[ ] ETAPA 5 — Criar FastAPI
[ ] ETAPA 6 — Conectar extensão + API
[ ] ETAPA 7 — Criar base de saúde
[ ] ETAPA 8 — Embeddings
[ ] ETAPA 9 — Busca semântica
[ ] ETAPA 10 — RAG
[ ] ETAPA 11 — LLM
[ ] ETAPA 12 — Classificação
[ ] ETAPA 13 — Evidências/fontes
[ ] ETAPA 14 — Pensamento crítico / UX
[ ] ETAPA 15 — Testes
[ ] ETAPA 16 — Métricas
[ ] ETAPA 17 — MLflow
[ ] ETAPA 18 — Limitações
[ ] ETAPA 19 — GitHub
[ ] ETAPA 20 — Demonstração
[ ] ETAPA 21 — Apresentação
```

E agora temos uma coisa muito importante: **dados estruturados**.

Antes o Google tinha apenas elementos HTML. Agora o HealthCheck consegue transformar esses elementos em informação que nosso sistema consegue processar:

```javascript
const dados = [
    {
        titulo: "Vitamina C previne gripe e resfriados?",
        url: "https://...",
        descricao: "A vitamina C não reduz..."
    },

    {
        titulo: "Vitamina C Para Diabetes",
        url: "https://...",
        descricao: "Descrição não encontrada"
    },

    // ...
];
```

Mais para frente, é justamente algo parecido com isso que vamos mandar para:

**Extensão → FastAPI → RAG → evidências → LLM → classificação → extensão.**

**ETAPA 3 CONCLUÍDA. ✅**

Agora podemos partir para a **ETAPA 4 — transformar aquele simples “🔎 HealthCheck IA — aguardando análise” em um selo/card visual profissional abaixo de cada resultado.**


-----------------------------------------------------------------------------------------------------------------------------------------------------


Perfeito. Na **ETAPA 4** vamos manter tudo simulado, mas deixar o HealthCheck IA com cara de produto de verdade.

A ideia é: em vez do texto simples “aguardando análise”, cada resultado do Google vai receber um **card visual**, com status, confiança e um botão expansível.

Substitua seu `content.js` por este:

```javascript
function capturarResultados() {
  const resultados = document.querySelectorAll("div.MjjYud");

  resultados.forEach((resultado, index) => {
    const tituloElemento = resultado.querySelector("h3");
    const linkElemento = tituloElemento?.closest("a");

    const descricaoElemento =
      resultado.querySelector(".VwiC3b") ||
      resultado.querySelector("[data-sncf]");

    if (!tituloElemento || !linkElemento) {
      return;
    }

    // Evita inserir o componente duas vezes
    if (resultado.querySelector(".healthcheck-card")) {
      return;
    }

    const item = {
      titulo: tituloElemento.innerText.trim(),
      url: linkElemento.href,
      descricao: descricaoElemento
        ? descricaoElemento.innerText.trim()
        : "Descrição não encontrada"
    };

    criarComponenteHealthCheck(resultado, item, index);
  });
}


function criarComponenteHealthCheck(resultado, item, index) {
  const card = document.createElement("div");

  card.className = "healthcheck-card";

  /*
    Por enquanto os resultados são SIMULADOS.

    Alternamos entre verde e vermelho apenas para
    visualizar os dois tipos de componente.
  */

  const favoravel = index % 2 === 0;

  const status = favoravel
    ? "Evidências favoráveis"
    : "Evidências contraditórias";

  const classeStatus = favoravel
    ? "healthcheck-favoravel"
    : "healthcheck-contraditorio";

  const icone = favoravel ? "🟢" : "🔴";

  const confianca = favoravel ? 85 : 82;

  card.innerHTML = `
    <div class="healthcheck-header">

      <div class="healthcheck-status ${classeStatus}">
        <span>${icone}</span>
        <strong>${status}</strong>
      </div>

      <div class="healthcheck-confidence">
        Confiança: ${confianca}%
      </div>

    </div>

    <button
      class="healthcheck-button"
      aria-expanded="false"
      type="button"
    >
      Ver análise
    </button>

    <div
      class="healthcheck-details"
      hidden
    >

      <p>
        Esta análise é apenas uma simulação visual.
        A Inteligência Artificial ainda não está conectada.
      </p>

      <p>
        <strong>Título analisado:</strong>
        ${item.titulo}
      </p>

      <p>
        <strong>Fonte:</strong>
        ${new URL(item.url).hostname}
      </p>

    </div>
  `;

  resultado.appendChild(card);

  const botao = card.querySelector(".healthcheck-button");
  const detalhes = card.querySelector(".healthcheck-details");

  botao.addEventListener("click", () => {
    const estaAberto =
      botao.getAttribute("aria-expanded") === "true";

    botao.setAttribute(
      "aria-expanded",
      String(!estaAberto)
    );

    detalhes.hidden = estaAberto;

    botao.innerText = estaAberto
      ? "Ver análise"
      : "Ocultar análise";
  });
}


function iniciarHealthCheck() {
  capturarResultados();
}


iniciarHealthCheck();


const observer = new MutationObserver(() => {
  capturarResultados();
});


observer.observe(document.body, {
  childList: true,
  subtree: true
});
```

Agora substitua seu `styles.css` por este:

```css
.healthcheck-card {
  margin-top: 12px;
  margin-bottom: 12px;

  padding: 14px 16px;

  max-width: 600px;

  background: #ffffff;

  border: 1px solid #dfe6ee;
  border-radius: 12px;

  box-shadow:
    0 2px 6px rgba(0, 0, 0, 0.06);

  font-family:
    Arial,
    Helvetica,
    sans-serif;

  color: #202124;
}


/* =========================
   CABEÇALHO
========================= */

.healthcheck-header {
  display: flex;

  justify-content: space-between;
  align-items: center;

  gap: 12px;

  margin-bottom: 12px;
}


/* =========================
   STATUS
========================= */

.healthcheck-status {
  display: flex;

  align-items: center;

  gap: 7px;

  font-size: 14px;
}


.healthcheck-favoravel {
  color: #137333;
}


.healthcheck-contraditorio {
  color: #b3261e;
}


/* =========================
   CONFIANÇA
========================= */

.healthcheck-confidence {
  font-size: 12px;

  color: #5f6368;

  background: #f1f3f4;

  padding: 5px 9px;

  border-radius: 20px;
}


/* =========================
   BOTÃO
========================= */

.healthcheck-button {
  background: transparent;

  border: none;

  padding: 0;

  color: #1a73e8;

  font-size: 13px;
  font-weight: 600;

  cursor: pointer;
}


.healthcheck-button:hover {
  text-decoration: underline;
}


.healthcheck-button:focus {
  outline: 2px solid #1a73e8;

  outline-offset: 3px;

  border-radius: 3px;
}


/* =========================
   DETALHES
========================= */

.healthcheck-details {
  margin-top: 14px;

  padding-top: 12px;

  border-top: 1px solid #e8eaed;

  font-size: 13px;

  line-height: 1.5;

  color: #3c4043;
}


.healthcheck-details p {
  margin: 7px 0;
}
```

Seu `manifest.json` pode continuar praticamente igual ao da etapa anterior:

```json
{
  "manifest_version": 3,
  "name": "HealthCheck IA",
  "version": "1.0",
  "description": "Extensão para auxiliar na verificação de informações de saúde.",
  "content_scripts": [
    {
      "matches": [
        "https://www.google.com/*"
      ],
      "js": [
        "content.js"
      ],
      "css": [
        "styles.css"
      ]
    }
  ]
}
```

Depois faça novamente:

```text
1. Salvar content.js
2. Salvar styles.css
3. Abrir chrome://extensions
4. Recarregar HealthCheck IA
5. Atualizar a página do Google
6. Fazer uma pesquisa
```

Você deverá começar a ver algo como:

```text
🟢 Evidências favoráveis             Confiança: 85%

Ver análise
```

No resultado seguinte:

```text
🔴 Evidências contraditórias         Confiança: 82%

Ver análise
```

E ao clicar em **Ver análise**, o card expande:

```text
Esta análise é apenas uma simulação visual.
A Inteligência Artificial ainda não está conectada.

Título analisado:
Vitamina C previne gripe e resfriados?

Fonte:
drauziovarella.uol.com.br

Ocultar análise
```

Um detalhe muito importante: os valores de **85% e 82% são totalmente falsos nesta etapa**. Eles estão ali apenas para construir e testar a interface. Não estamos calculando nada ainda.

O `index % 2 === 0` também não faz qualquer análise:

```javascript
const favoravel = index % 2 === 0;
```

Ele simplesmente faz:

```text
Resultado 1 → verde
Resultado 2 → vermelho
Resultado 3 → verde
Resultado 4 → vermelho
...
```

É só para conseguirmos enxergar os dois estados da interface.

Nesta etapa você está aprendendo cinco conceitos importantes ao mesmo tempo: **componentes**, porque cada resultado recebe um card independente; **eventos**, porque o botão reage ao clique; **estado**, porque o componente sabe se está aberto ou fechado; **CSS**, porque estamos estilizando elementos inseridos pela extensão; e **acessibilidade**, usando `button`, `aria-expanded`, foco visual e `hidden`.

Quando estiver funcionando, nossa arquitetura já terá evoluído para:

```text
Google
   ↓
HealthCheck encontra o resultado
   ↓
captura título + URL + descrição
   ↓
cria componente visual
   ↓
┌──────────────────────────────┐
│ 🟢 Evidências favoráveis     │
│ Confiança: 85%               │
│                              │
│ Ver análise                  │
└──────────────────────────────┘
```

Aí podemos marcar **ETAPA 4 concluída** e partir para a primeira parte fora do navegador: **ETAPA 5 — criar nossa API em Python com FastAPI.**


--------------------------------------------------------------------------------------------------------------------------------------------------------------

Sim, **ficaria exatamente assim**. Esse `content.js` está coerente com a correção que fizemos.

Só tem um detalhe: do jeito que está, o `contadorResultados` reinicia em `0` toda vez que o `MutationObserver` chama `capturarResultados()`. Como os cards já inseridos são ignorados, isso não costuma quebrar, mas pode fazer novos resultados carregados depois começarem novamente pelo verde.

Para a **Etapa 4**, isso é totalmente aceitável. O importante é você ver agora a alternância:

```text
🟢 Evidências favoráveis — 85%
🔴 Evidências contraditórias — 82%
🟢 Evidências favoráveis — 85%
🔴 Evidências contraditórias — 82%
```

Então faça:

```text
1. Salve o content.js
2. Vá em chrome://extensions
3. Recarregue o HealthCheck IA
4. Volte ao Google
5. Atualize a página com Ctrl + F5
6. Faça a pesquisa novamente
```

Se aparecer verde e vermelho alternando, a **ETAPA 4 está concluída**. ✅

E reforçando: esses resultados ainda são **simulados**. O código:

```javascript
const favoravel = index % 2 === 0;
```

não está analisando se a notícia é verdadeira ou falsa. Ele só usa a posição do resultado para escolher a aparência do card. A análise real só vai surgir quando conectarmos o backend e, mais adiante, o RAG/LLM.


----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

Perfeito. Agora entramos na **ETAPA 5 — Backend Python com FastAPI**.

A ideia é criar um servidor separado da extensão. A extensão será a interface no navegador; o backend será onde ficará a lógica do sistema.

Crie uma pasta nova ao lado da extensão:

```text
healthcheck-ia/
│
├── extension/
│   ├── manifest.json
│   ├── content.js
│   └── styles.css
│
└── backend/
    └── main.py
```

Dentro da pasta `backend`, abra o terminal e instale:

```bash
pip install fastapi uvicorn
```

Agora crie o arquivo `main.py`:

```python
from fastapi import FastAPI
from pydantic import BaseModel
from fastapi.middleware.cors import CORSMiddleware

app = FastAPI(
    title="HealthCheck IA API",
    version="1.0.0"
)


# =========================
# CORS
# =========================

app.add_middleware(
    CORSMiddleware,
    allow_origins=["*"],
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)


# =========================
# MODELOS DE DADOS
# =========================

class AnaliseRequest(BaseModel):
    titulo: str
    descricao: str


class AnaliseResponse(BaseModel):
    classificacao: str
    confianca: float


# =========================
# ROTAS
# =========================

@app.get("/")
def raiz():
    return {
        "mensagem": "HealthCheck IA API funcionando"
    }


@app.post(
    "/analisar",
    response_model=AnaliseResponse
)
def analisar(dados: AnaliseRequest):

    print("Título recebido:", dados.titulo)
    print("Descrição recebida:", dados.descricao)

    # RESPOSTA SIMULADA
    return AnaliseResponse(
        classificacao="enganosa",
        confianca=0.82
    )
```

Depois, dentro da pasta `backend`, rode:

```bash
uvicorn main:app --reload
```

Se funcionar, você deverá ver algo parecido com:

```text
Uvicorn running on http://127.0.0.1:8000
```

Abra no navegador:

```text
http://127.0.0.1:8000
```

Deve aparecer:

```json
{
  "mensagem": "HealthCheck IA API funcionando"
}
```

Agora vem uma parte muito útil do FastAPI. Abra:

```text
http://127.0.0.1:8000/docs
```

Você verá automaticamente uma documentação interativa da API.

Procure:

```text
POST /analisar
```

Clique em:

```text
Try it out
```

E envie:

```json
{
  "titulo": "Vitamina C cura gripe?",
  "descricao": "A vitamina C pode curar gripe rapidamente."
}
```

A resposta esperada será:

```json
{
  "classificacao": "enganosa",
  "confianca": 0.82
}
```

Se isso acontecer, teremos conseguido:

```text
CLIENTE
   ↓
HTTP POST
   ↓
JSON
   ↓
FASTAPI
   ↓
PYDANTIC
   ↓
função analisar()
   ↓
JSON
   ↓
RESPOSTA
```

Agora, os conceitos importantes desta etapa.

**API** é a interface pela qual dois sistemas se comunicam. No nosso caso, futuramente:

```text
Chrome Extension
       ↓
      API
       ↓
Backend Python
```

**HTTP** é o protocolo usado nessa comunicação.

**GET** normalmente busca informações:

```http
GET /
```

**POST** envia informações para processamento:

```http
POST /analisar
```

Por isso usamos POST para análise. Estamos enviando conteúdo para o backend processar.

O corpo da requisição será JSON:

```json
{
  "titulo": "...",
  "descricao": "..."
}
```

E o backend responde também em JSON:

```json
{
  "classificacao": "enganosa",
  "confianca": 0.82
}
```

O **Pydantic** entra aqui:

```python
class AnaliseRequest(BaseModel):
    titulo: str
    descricao: str
```

Ele define o contrato de entrada.

Se alguém mandar:

```json
{
  "titulo": 123,
  "descricao": []
}
```

o FastAPI/Pydantic pode rejeitar os dados porque não correspondem ao formato esperado.

Isso é importante porque nosso sistema não deve simplesmente aceitar qualquer estrutura.

Já:

```python
class AnaliseResponse(BaseModel):
    classificacao: str
    confianca: float
```

define o formato esperado da resposta.

E o **CORS**:

```python
app.add_middleware(
    CORSMiddleware,
    allow_origins=["*"],
    allow_methods=["*"],
    allow_headers=["*"],
)
```

será importante quando a extensão Chrome começar a fazer requisições para:

```text
http://127.0.0.1:8000
```

Nesta etapa estamos liberando tudo porque é um ambiente local de desenvolvimento. Mais tarde podemos restringir isso.

E uma observação essencial:

```python
return AnaliseResponse(
    classificacao="enganosa",
    confianca=0.82
)
```

**não existe nenhuma inteligência aí ainda.**

Todo texto enviado receberá:

```text
enganosa
82%
```

Mesmo se você mandar:

```json
{
  "titulo": "A Terra é redonda",
  "descricao": "..."
}
```

ele continuará respondendo:

```json
{
  "classificacao": "enganosa",
  "confianca": 0.82
}
```

Porque estamos apenas construindo a infraestrutura.

Nossa arquitetura agora começa a ficar assim:

```text
┌─────────────────────┐
│       GOOGLE        │
└──────────┬──────────┘
           ↓
┌─────────────────────┐
│ CHROME EXTENSION    │
│ JavaScript          │
└─────────────────────┘


┌─────────────────────┐
│ BACKEND             │
│ Python + FastAPI    │
│                     │
│ POST /analisar      │
└─────────────────────┘
```

Ainda estão separados.

A **ETAPA 6** vai conectar os dois:

```text
Google
  ↓
Extensão
  ↓
fetch()
  ↓
POST /analisar
  ↓
FastAPI
  ↓
JSON
  ↓
Extensão
  ↓
Card do HealthCheck
```

Para concluir a **ETAPA 5**, precisamos confirmar três coisas:

```text
[ ] http://127.0.0.1:8000 abre

[ ] http://127.0.0.1:8000/docs abre

[ ] POST /analisar retorna:
    classificacao = enganosa
    confianca = 0.82
```

Se essas três coisas funcionarem, marcamos **ETAPA 5 concluída**.

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

Perfeito. Agora vamos para a **ETAPA 6 — conectar a extensão Chrome com o backend Python**.

Aqui acontece a primeira comunicação real entre o `content.js` e o FastAPI.

Seu backend precisa estar rodando com:

```bash
uvicorn main:app --reload
```

e acessível em:

```text
http://127.0.0.1:8000
```

Agora vamos alterar o `content.js`.

A ideia será esta:

```text
resultado do Google
↓
captura título + descrição
↓
fetch()
↓
POST /analisar
↓
FastAPI
↓
JSON
↓
JavaScript recebe
↓
card muda sozinho
```

Use este `content.js`:

```javascript
async function analisarResultado(item) {
  try {
    const resposta = await fetch("http://127.0.0.1:8000/analisar", {
      method: "POST",

      headers: {
        "Content-Type": "application/json"
      },

      body: JSON.stringify({
        titulo: item.titulo,
        descricao: item.descricao
      })
    });

    if (!resposta.ok) {
      throw new Error(`Erro HTTP: ${resposta.status}`);
    }

    const dados = await resposta.json();

    console.log("Resposta do backend:", dados);

    return dados;

  } catch (erro) {
    console.error("Erro ao conectar com o backend:", erro);

    return null;
  }
}


async function capturarResultados() {
  const resultados = document.querySelectorAll("div.MjjYud");

  for (const resultado of resultados) {

    const tituloElemento = resultado.querySelector("h3");
    const linkElemento = tituloElemento?.closest("a");

    const descricaoElemento =
      resultado.querySelector(".VwiC3b") ||
      resultado.querySelector("[data-sncf]");

    if (!tituloElemento || !linkElemento) {
      continue;
    }

    if (resultado.querySelector(".healthcheck-card")) {
      continue;
    }

    const item = {
      titulo: tituloElemento.innerText.trim(),

      url: linkElemento.href,

      descricao: descricaoElemento
        ? descricaoElemento.innerText.trim()
        : "Descrição não encontrada"
    };

    criarComponenteHealthCheck(resultado, item);
  }
}


async function criarComponenteHealthCheck(resultado, item) {

  const card = document.createElement("div");

  card.className = "healthcheck-card";

  /*
    Primeiro mostramos o estado de carregamento.
  */

  card.innerHTML = `
    <div class="healthcheck-header">

      <div class="healthcheck-status">
        <span>🔎</span>

        <strong>
          HealthCheck IA
        </strong>
      </div>

      <div class="healthcheck-confidence">
        Analisando...
      </div>

    </div>

    <div class="healthcheck-loading">
      Consultando o backend...
    </div>
  `;

  resultado.appendChild(card);


  /*
    Agora enviamos o conteúdo para o FastAPI.
  */

  const analise = await analisarResultado(item);


  /*
    Caso não seja possível conectar ao backend.
  */

  if (!analise) {

    card.innerHTML = `
      <div class="healthcheck-header">

        <div class="healthcheck-status healthcheck-erro">
          <span>⚠️</span>

          <strong>
            Não foi possível analisar
          </strong>
        </div>

      </div>

      <p>
        O servidor HealthCheck IA não respondeu.
      </p>
    `;

    return;
  }


  /*
    Por enquanto nosso backend sempre retorna:

    {
      classificacao: "enganosa",
      confianca: 0.82
    }

    Portanto ainda é uma resposta simulada.
  */


  const classificacao = analise.classificacao;

  const confianca = Math.round(
    analise.confianca * 100
  );


  let status;
  let icone;
  let classeStatus;


  if (classificacao === "enganosa") {

    status = "Evidências contraditórias";

    icone = "🔴";

    classeStatus = "healthcheck-contraditorio";

  } else {

    status = "Resultado recebido";

    icone = "🟡";

    classeStatus = "";
  }


  card.innerHTML = `
    <div class="healthcheck-header">

      <div class="healthcheck-status ${classeStatus}">

        <span>
          ${icone}
        </span>

        <strong>
          ${status}
        </strong>

      </div>

      <div class="healthcheck-confidence">

        Confiança:
        ${confianca}%

      </div>

    </div>


    <button
      class="healthcheck-button"
      aria-expanded="false"
      type="button"
    >

      Ver análise

    </button>


    <div
      class="healthcheck-details"
      hidden
    >

      <p>

        <strong>
          Classificação recebida do backend:
        </strong>

        ${classificacao}

      </p>


      <p>

        <strong>
          Título analisado:
        </strong>

        ${item.titulo}

      </p>


      <p>

        <strong>
          Fonte:
        </strong>

        ${new URL(item.url).hostname}

      </p>


      <p class="healthcheck-warning">

        Esta classificação ainda é simulada.
        A Inteligência Artificial ainda não foi implementada.

      </p>

    </div>
  `;


  const botao =
    card.querySelector(".healthcheck-button");

  const detalhes =
    card.querySelector(".healthcheck-details");


  botao.addEventListener("click", () => {

    const estaAberto =
      botao.getAttribute("aria-expanded") === "true";


    botao.setAttribute(
      "aria-expanded",
      String(!estaAberto)
    );


    detalhes.hidden = estaAberto;


    botao.innerText = estaAberto
      ? "Ver análise"
      : "Ocultar análise";

  });
}



capturarResultados();


const observer = new MutationObserver(() => {

  capturarResultados();

});


observer.observe(
  document.body,
  {
    childList: true,
    subtree: true
  }
);
```

Seu `styles.css` pode continuar praticamente igual. Só acrescente no final:

```css
.healthcheck-loading {
  font-size: 13px;
  color: #5f6368;
}

.healthcheck-erro {
  color: #b3261e;
}

.healthcheck-warning {
  margin-top: 12px;
  padding: 10px;

  background: #fff8e1;

  border-left: 3px solid #f9ab00;

  border-radius: 4px;
}
```

Agora precisamos fazer uma pequena alteração no `manifest.json`, porque a extensão vai acessar nosso servidor local.

Deixe assim:

```json
{
  "manifest_version": 3,

  "name": "HealthCheck IA",

  "version": "1.0",

  "description": "Extensão para auxiliar na verificação de informações de saúde.",

  "host_permissions": [
    "http://127.0.0.1:8000/*"
  ],

  "content_scripts": [
    {
      "matches": [
        "https://www.google.com/*"
      ],

      "js": [
        "content.js"
      ],

      "css": [
        "styles.css"
      ]
    }
  ]
}
```

Agora faça nessa ordem:

1. Deixe o FastAPI ligado:

```bash
uvicorn main:app --reload
```

2. Confirme que:

```text
http://127.0.0.1:8000
```

está funcionando.

3. Salve `content.js`.

4. Salve `styles.css`.

5. Salve `manifest.json`.

6. Vá em:

```text
chrome://extensions
```

7. Clique em **Recarregar** no HealthCheck IA.

8. Volte ao Google e dê:

```text
Ctrl + F5
```

9. Pesquise novamente:

```text
vitamina C cura gripe
```

Agora observe o card.

Ele deve aparecer primeiro assim:

```text
🔎 HealthCheck IA                Analisando...

Consultando o backend...
```

e depois mudar automaticamente para:

```text
🔴 Evidências contraditórias     Confiança: 82%

Ver análise
```

Isso será muito diferente da Etapa 4.

Na Etapa 4, o JavaScript inventava o resultado:

```javascript
const confianca = 82;
```

Agora não.

Quem envia isso é o Python:

```python
return AnaliseResponse(
    classificacao="enganosa",
    confianca=0.82
)
```

O navegador recebe aqui:

```javascript
const dados = await resposta.json();
```

Portanto teremos uma comunicação real:

```text
Chrome

content.js
   ↓
fetch()
   ↓

HTTP POST

{
 "titulo": "...",
 "descricao": "..."
}

   ↓

FastAPI

POST /analisar

   ↓

Python

   ↓

{
 "classificacao": "enganosa",
 "confianca": 0.82
}

   ↓

HTTP Response

   ↓

JavaScript

   ↓

Math.round(0.82 * 100)

   ↓

82%

   ↓

CARD DO GOOGLE
```

Um detalhe importante: como o backend atualmente retorna `"enganosa"` para qualquer entrada, **todos os cards provavelmente ficarão vermelhos com 82% novamente**.

Mas agora isso estará correto.

Antes:

```text
vermelho → inventado pelo JavaScript
```

Agora:

```text
vermelho
   ↑
JSON
   ↑
FastAPI
   ↑
Python
```

É exatamente isso que queremos comprovar na Etapa 6.

Para verificar ainda melhor, abra o `F12 → Console`.

Você deve encontrar mensagens como:

```text
Resposta do backend:
{
    classificacao: "enganosa",
    confianca: 0.82
}
```

E no terminal onde está o FastAPI provavelmente aparecerão chamadas como:

```text
POST /analisar HTTP/1.1 200 OK
```

Se o card mudar de **“Analisando...”** para **“Evidências contraditórias — Confiança: 82%”**, e o backend registrar os `POST /analisar`, então podemos declarar:

**[X] ETAPA 6 — Chrome ↔ Python concluída. ✅**


------------------------------------------------------------------------------------------------------------------------------------

UHUUU! 🚀 **ETAPA 6 oficialmente concluída!** E essa foi uma etapa importante porque agora vocês têm uma arquitetura **cliente-servidor funcionando de verdade**.

```text
HEALTHCHECK IA — PROGRESSO

[X] ETAPA 1 — Definir o MVP
[X] ETAPA 2 — Extensão Chrome básica
[X] ETAPA 3 — Capturar resultados do Google
[X] ETAPA 4 — Componente visual
[X] ETAPA 5 — Backend Python + FastAPI
[X] ETAPA 6 — Conectar Chrome ↔ Python ✅

[ ] ETAPA 7 — Base de evidências de saúde
[ ] ETAPA 8 — Embeddings
[ ] ETAPA 9 — Busca semântica
[ ] ETAPA 10 — RAG
[ ] ETAPA 11 — LLM local
[ ] ETAPA 12 — Classificação
[ ] ETAPA 13 — Evidências e fontes
[ ] ETAPA 14 — Pensamento crítico / UX
[ ] ETAPA 15 — Testes
[ ] ETAPA 16 — Métricas
[ ] ETAPA 17 — MLflow
...
```

O sistema já executa este fluxo real:

```text
GOOGLE
   ↓
Extensão Chrome
   ↓
Captura título + descrição + URL
   ↓
JavaScript
   ↓
fetch()
   ↓
HTTP POST
   ↓
FastAPI / Python
   ↓
POST /analisar
   ↓
JSON
   ↓
Extensão recebe a resposta
   ↓
Atualiza o card no Google
```

Até agora, a única parte "falsa" é **a inteligência**. O backend ainda responde manualmente:

```json
{
  "classificacao": "enganosa",
  "confianca": 0.82
}
```

Mas a infraestrutura necessária para substituir isso por uma análise real já está funcionando.

A **ETAPA 7 — Base de evidências de saúde** começa justamente essa mudança. Em vez de o Python simplesmente inventar `"enganosa"`, vamos começar a construir o conhecimento confiável que posteriormente será recuperado pelo RAG.

Aí entram fontes como **Ministério da Saúde, Anvisa, Fiocruz, BVS, PubMed, universidades e sociedades médicas**, com uma preocupação importante: guardar não apenas o texto, mas também **fonte, título, URL, data e outros metadados**, para que futuramente o HealthCheck consiga mostrar ao usuário **qual evidência sustentou a análise**.

**6 etapas concluídas. Agora começa a construção da parte realmente inteligente do HealthCheck IA. 🔥**


------------------------------------------------------------------------------------------------------------------------------------------------------------

Perfeito. A **ETAPA 7** deve começar pequena e controlada. Antes de automatizar coleta de sites ou PubMed, o melhor é montar uma **base local confiável e rastreável**, com poucos documentos, mas bem organizados.

A estrutura pode ficar assim:

```text
healthcheck-extension/
│
├── backend/
│   └── main.py
│
├── extension/
│   ├── manifest.json
│   ├── content.js
│   └── styles.css
│
└── data/
    ├── vacinas/
    ├── diabetes/
    ├── hipertensao/
    ├── cancer/
    ├── dengue/
    └── medicamentos/
```

Dentro de cada pasta, inicialmente eu sugiro usar arquivos `.txt` ou `.md`, porque são simples de ler com Python e depois transformar em embeddings.

Por exemplo:

```text
data/
└── vacinas/
    ├── ministerio_saude_vacina_gripe.md
    ├── fiocruz_vacinas.md
    └── anvisa_vacinas.md
```

Um documento pode ter este formato:

```markdown
# Vacina contra influenza

Fonte: Ministério da Saúde
URL: https://...
Data de acesso: 09/09/2026
Tema: Vacinas

## Conteúdo

A vacina contra influenza é utilizada para reduzir o risco de formas graves da doença e suas complicações.

A vacina não causa gripe, pois as vacinas utilizadas na campanha não contêm vírus influenza capazes de provocar a doença.

## Palavras-chave

influenza, gripe, vacina, vacinação, prevenção
```

Esse formato é importante porque não queremos guardar apenas o texto. Precisamos preservar também os **metadados**:

```text
conteúdo
fonte
URL
tema
data
título
```

Mais tarde, quando o HealthCheck encontrar uma evidência, ele poderá devolver algo como:

```json
{
  "evidencia": "A vacina contra influenza não causa gripe...",
  "fonte": "Ministério da Saúde",
  "url": "https://...",
  "tema": "vacinas"
}
```

E isso vai permitir que o usuário clique em **Ver evidências** e veja de onde a informação veio.

### O primeiro conjunto da base

Eu não tentaria agora colocar centenas de documentos. Para o MVP, começaria com algo como:

```text
VACINAS
3 a 5 documentos

DIABETES
3 a 5 documentos

HIPERTENSÃO
3 a 5 documentos

DENGUE
3 a 5 documentos

MEDICAMENTOS
3 a 5 documentos
```

Isso já nos daria algo perto de **15 a 25 documentos confiáveis** para testar o sistema.

Cancer pode entrar depois, porque é um assunto extremamente amplo.

Outra coisa importante: **não precisamos armazenar artigos científicos inteiros**. Para o MVP, podemos guardar trechos relevantes, resumos ou documentos públicos selecionados, sempre mantendo a referência da fonte.

### Antes dos embeddings

Nesta etapa ainda não vamos fazer busca semântica. Primeiro precisamos provar que o Python consegue ler nossa base.

Depois de criar alguns arquivos, podemos fazer algo simples como:

```python
from pathlib import Path

pasta_data = Path("../data")

documentos = []

for arquivo in pasta_data.rglob("*.md"):
    conteudo = arquivo.read_text(
        encoding="utf-8"
    )

    documentos.append({
        "arquivo": arquivo.name,
        "conteudo": conteudo
    })

print(f"Documentos carregados: {len(documentos)}")

for documento in documentos:
    print(documento["arquivo"])
```

Se tivermos:

```text
data/
├── vacinas/
│   ├── vacina_gripe.md
│   └── vacina_covid.md
│
└── diabetes/
    └── diabetes_tipo2.md
```

o Python poderia mostrar:

```text
Documentos carregados: 3

vacina_gripe.md
vacina_covid.md
diabetes_tipo2.md
```

Nesse ponto teremos:

```text
FONTES CONFIÁVEIS
        ↓
documentos selecionados
        ↓
data/
        ↓
Python lê os arquivos
        ↓
lista de documentos
```

Na **Etapa 8**, essa lista será transformada em **embeddings**, ou seja, representações vetoriais que permitirão buscar informações por significado e não apenas por palavras exatas.

Para concluir a **ETAPA 7**, eu colocaria estas metas:

```text
[ ] Criar pasta data/

[ ] Definir temas iniciais

[ ] Selecionar fontes confiáveis

[ ] Criar pelo menos 10 documentos

[ ] Cada documento possuir:
    - título
    - fonte
    - URL
    - tema
    - conteúdo

[ ] Python conseguir carregar todos os documentos
```

Aqui tem uma diferença importante para as etapas anteriores: **não vale inventar o conteúdo científico dos arquivos**. Os textos precisam vir das fontes confiáveis que vocês selecionarem.

Se quiser, podemos fazer essa etapa juntos começando por **vacinas**, montando os primeiros documentos reais da base e o código Python que vai carregá-los.

--------------------------------------------------------------------------------------------

Então fechamos essa parte com:

```text
Documentos encontrados: 18
Com conteúdo: 18
Vazios: 0
Estrutura válida: 18
Com problemas de estrutura: 0
```

Isso é um marco importante porque agora não temos simplesmente “18 arquivos”. Temos uma **base de conhecimento estruturada e validada pelo nosso próprio código**.

O fluxo já está assim:

```text
18 fontes de saúde (.md)
        ↓
6 categorias
        ↓
carregar_documentos.py
        ↓
verificação de arquivo vazio
        ↓
verificação dos campos obrigatórios
        ↓
18 documentos válidos ✅
```

### Onde estamos

```text
[X] 01 — Definir MVP
[X] 02 — Extensão Chrome
[X] 03 — Capturar resultados Google
[X] 04 — Cards visuais
[X] 05 — Backend FastAPI
[X] 06 — Extensão ↔ Backend
[X] 07 — Base de conhecimento ✅

[ ] 08 — EMBEDDINGS  ← PRÓXIMO
[ ] 09 — Busca semântica/vetorial
[ ] 10 — RAG
[ ] 11 — LLM local
[ ] 12 — Classificação
[ ] 13 — Evidências/fontes
[ ] 14 — UX de pensamento crítico
...
```

E agora começa uma parte **muito legal da IA**: **embeddings**.

Vamos pegar aqueles textos como:

```text
"A vacinação contra influenza é realizada anualmente..."
```

e transformar o significado deles em **vetores numéricos**:

```text
Texto
 ↓
Modelo de Embeddings
 ↓
[0.018, -0.273, 0.419, 0.052, ...]
```

Isso permitirá que depois alguém pesquise algo como:

> “tomar vacina da gripe todo ano é desnecessário”

e o sistema consiga encontrar um documento sobre **vacinação anual contra influenza**, mesmo que a frase pesquisada não seja idêntica ao texto do documento.

Esse é o fundamento da nossa futura **busca semântica e do RAG**.

**Próxima etapa: 08 — Embeddings.** Vamos fazer do mesmo jeito: uma coisa de cada vez, entendendo o que estamos fazendo antes de colocar código. 🚀

--------------------------------------------------------------------------------------------------------------------------------------------------------------------------

Exatamente. 🚀 **Agora começamos oficialmente a ETAPA 8 — Embeddings + Busca Semântica.**

E eu sugiro não instalar nada ainda. Primeiro precisamos entender o mecanismo, porque daqui para frente essas ideias aparecem em praticamente todo o nosso RAG.

## 8.1 — O problema que estamos resolvendo

Hoje temos 18 documentos, mas o Python basicamente sabe que eles são **strings**.

Imagine que nossa base contenha:

```text
Não existem evidências científicas de que vitamina C cure infecções por influenza.
```

E alguém pesquise:

```text
Tomar vitamina C acaba com a gripe?
```

Uma busca tradicional por palavras pode ter dificuldade porque:

```text
Documento: "cure infecções por influenza"
Pesquisa:  "acaba com a gripe"
```

Mas semanticamente elas tratam de conceitos muito próximos.

É aí que entram os **embeddings**.

## 8.2 — O que é um embedding?

Um modelo de embeddings recebe um texto e produz um **vetor numérico de dimensão fixa** que representa características semânticas desse texto.

Conceitualmente:

```text
"Vacina ajuda a prevenir formas graves da gripe"
                     ↓
             Modelo de embedding
                     ↓
[0.12, -0.41, 0.08, 0.73, -0.19, ...]
```

Esse vetor pode possuir centenas de dimensões.

Não devemos interpretar isso como:

```text
0.12 = vacina
-0.41 = gripe
0.08 = prevenção
```

Não funciona assim.

As dimensões são representações aprendidas pelo modelo durante seu treinamento. O importante para nós é a **geometria do espaço vetorial**: textos semanticamente relacionados tendem a produzir representações próximas.

Por exemplo:

```text
A = "Vacinação reduz casos graves de gripe."

B = "A vacina ajuda a evitar complicações da influenza."

C = "O tratamento do câncer depende do tipo e estágio da doença."
```

Esperamos que:

```text
A ↔ B     muito próximos

A ↔ C     menos próximos
```

## 8.3 — Como sabemos que dois vetores são próximos?

Entrará outro conceito da sua lista:

**similaridade de cosseno**.

Se temos dois vetores:

```text
A = embedding do documento
B = embedding da pesquisa
```

podemos calcular:

```text
cos(A, B)
```

De forma simplificada, a fórmula é:

$$
\text{similaridade}(A,B)=
\frac{A\cdot B}
{\|A\|\|B\|}
$$

Ela compara essencialmente a orientação dos vetores no espaço.

Quanto maior a similaridade, maior tende a ser a relação semântica entre os textos, dependendo do modelo e da forma como os embeddings foram normalizados.

Então poderíamos ter algo conceitualmente parecido com:

```text
Pesquisa:
"Tomar vitamina C acaba com a gripe?"

Documento A → similaridade 0.82
Documento B → similaridade 0.69
Documento C → similaridade 0.21
```

Ordenamos:

```text
1º Documento A
2º Documento B
3º Documento C
```

E selecionamos os resultados mais relevantes.

## 8.4 — Onde entra o Sentence Transformers?

Precisamos de um modelo capaz de transformar nossos textos em embeddings.

Para nosso MVP, podemos usar a biblioteca [Sentence Transformers](https://www.sbert.net/?utm_source=chatgpt.com), que é open source.

Em Python, futuramente teremos algo conceitualmente assim:

```python
modelo.encode("Vacinação reduz casos graves de influenza.")
```

e receberemos:

```text
texto
 ↓
Sentence Transformer
 ↓
vetor
```

Não vamos precisar **treinar um modelo de embeddings do zero**.

Usaremos um modelo já pré-treinado.

## 8.5 — E o chunking?

Esse conceito é extremamente importante para nosso HealthCheck.

Imagine um documento enorme:

```text
ministerio_saude_dengue.md
```

com várias partes:

```text
Definição
Transmissão
Sintomas
Sinais de alarme
Prevenção
Tratamento
Vacinação
```

Não é necessariamente interessante transformar tudo isso em **um único embedding**.

Podemos dividir o documento em partes menores:

```text
DOCUMENTO
   ↓
CHUNK 1
"Dengue é uma arbovirose..."

CHUNK 2
"A transmissão ocorre principalmente..."

CHUNK 3
"Os principais sintomas..."

CHUNK 4
"Os sinais de alarme incluem..."

CHUNK 5
"A prevenção envolve..."
```

Cada chunk recebe seu próprio embedding:

```text
Chunk 1 → vetor
Chunk 2 → vetor
Chunk 3 → vetor
Chunk 4 → vetor
Chunk 5 → vetor
```

Isso melhora a granularidade da recuperação.

Se alguém pesquisar:

```text
"Dor abdominal pode ser sinal de dengue grave?"
```

não precisamos recuperar necessariamente o documento inteiro.

A busca poderá encontrar diretamente o **chunk sobre sinais de alarme**.

## 8.6 — Nosso pipeline começa a aparecer

Estamos construindo isto:

```text
                 BASE DE CONHECIMENTO
                         │
                         ▼
                 18 documentos .md
                         │
                         ▼
                      CHUNKS
                         │
                         ▼
              MODELO DE EMBEDDINGS
                         │
                         ▼
                     VETORES
                         │
                         ▼
                  ÍNDICE VETORIAL
                         ▲
                         │
                 embedding da busca
                         ▲
                         │
                  PESQUISA DO USUÁRIO
```

Depois:

```text
Pesquisa
   ↓
Embedding da pesquisa
   ↓
Comparação com embeddings da base
   ↓
Similaridade
   ↓
Top-K chunks mais semelhantes
   ↓
Evidências relevantes
```

E **só mais tarde**:

```text
Evidências recuperadas
        +
Alegação/notícia
        ↓
       LLM
        ↓
Análise fundamentada
```

Isso será o nosso **RAG**.

### Onde estamos exatamente

```text
ETAPA 8 — EMBEDDINGS + BUSCA SEMÂNTICA

[ ] 8.1 Entender embeddings        ← entendemos agora
[ ] 8.2 Preparar os documentos
[ ] 8.3 Fazer chunking
[ ] 8.4 Instalar Sentence Transformers
[ ] 8.5 Escolher modelo
[ ] 8.6 Gerar primeiro embedding
[ ] 8.7 Gerar embeddings da base
[ ] 8.8 Calcular similaridade de cosseno
[ ] 8.9 Fazer primeira busca semântica
[ ] 8.10 Integrar ao backend
```

**Próximo passo: 8.2/8.3 — preparar os nossos 18 documentos para chunking.**

E aqui eu quero fazer uma coisa importante: **não vamos começar com FAISS nem banco vetorial ainda**. Primeiro vamos conseguir pegar uma pergunta e encontrar semanticamente o trecho correto usando Python puro + Sentence Transformers. Quando isso estiver funcionando e você entender o mecanismo, colocamos a camada de busca vetorial. Isso deixa muito mais claro o que cada tecnologia está fazendo.


------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------


Perfeito. Agora vamos fazer o **8.2/8.3 sem pular etapa**: primeiro preparar os documentos, depois transformar cada um em pequenos trechos chamados **chunks**.

O objetivo é sair de:

```text
1 arquivo grande
```

para algo assim:

```text
arquivo
  ↓
chunk 1
chunk 2
chunk 3
chunk 4
```

E cada chunk precisa continuar sabendo **de qual arquivo e fonte ele veio**.

Por enquanto, não vamos gerar embedding nenhum.

Abra seu:

```text
backend/carregar_documentos.py
```

Nós vamos aproveitar a lista `documentos` que já existe e adicionar uma função de chunking.

Abaixo desta parte:

```python
documentos_invalidos = []
```

adicione:

```python
def dividir_em_chunks(texto, tamanho_maximo=500):
    paragrafos = texto.split("\n\n")

    chunks = []
    chunk_atual = ""

    for paragrafo in paragrafos:
        paragrafo = paragrafo.strip()

        if not paragrafo:
            continue

        if len(chunk_atual) + len(paragrafo) <= tamanho_maximo:
            chunk_atual += paragrafo + "\n\n"

        else:
            if chunk_atual:
                chunks.append(chunk_atual.strip())

            chunk_atual = paragrafo + "\n\n"

    if chunk_atual:
        chunks.append(chunk_atual.strip())

    return chunks
```

Essa função recebe um texto e devolve vários blocos menores.

Por exemplo:

```python
texto = """
Dengue é uma arbovirose.

A transmissão ocorre principalmente
pela picada do Aedes aegypti.

A doença pode apresentar sinais
de alarme.
"""
```

Pode virar:

```text
CHUNK 1
Dengue é uma arbovirose.

CHUNK 2
A transmissão ocorre principalmente
pela picada do Aedes aegypti.

CHUNK 3
A doença pode apresentar sinais
de alarme.
```

Mas existe um detalhe importante: não estamos simplesmente quebrando a cada 500 caracteres.

Estamos tentando preservar **parágrafos inteiros**.

Isso é melhor do que cortar assim:

```text
A transmissão da dengue ocorre pela picada da fêmea
do mosquito Aedes ae
```

e depois:

```text
gypti infectada pelo vírus.
```

Esse corte destruiria parte do contexto.

Agora, depois de terminar o carregamento dos documentos, adicione:

```python
chunks = []

for documento in documentos:
    partes = dividir_em_chunks(documento["conteudo"])

    for indice, parte in enumerate(partes):
        chunks.append({
            "arquivo": documento["arquivo"],
            "chunk_id": indice,
            "conteudo": parte
        })
```

Agora criamos uma segunda estrutura.

Antes:

```python
documentos
```

poderia ser:

```python
[
    {
        "arquivo": "ministerio_saude_dengue.md",
        "conteudo": "..."
    }
]
```

Agora teremos também:

```python
chunks
```

com algo parecido com:

```python
[
    {
        "arquivo": "ministerio_saude_dengue.md",
        "chunk_id": 0,
        "conteudo": "Dengue é uma arbovirose..."
    },

    {
        "arquivo": "ministerio_saude_dengue.md",
        "chunk_id": 1,
        "conteudo": "A transmissão ocorre..."
    }
]
```

Isso é muito importante para o futuro.

Quando a busca semântica encontrar:

```text
chunk 4
```

vamos saber:

```text
qual texto foi encontrado
qual documento originou o texto
qual posição ele ocupa
```

No final do arquivo, acrescente:

```python
print(f"\nTotal de chunks gerados: {len(chunks)}")

print("\nExemplo dos primeiros chunks:")

for chunk in chunks[:5]:
    print("\n----------------------------")
    print(f"Arquivo: {chunk['arquivo']}")
    print(f"Chunk: {chunk['chunk_id']}")
    print(chunk["conteudo"][:300])
```

Então, conceitualmente, seu programa passa a fazer:

```text
data/
  ↓
18 arquivos .md
  ↓
carregar documentos
  ↓
validar estrutura
  ↓
dividir em chunks
  ↓
lista de chunks
```

Um ponto técnico importante: o `500` aqui:

```python
tamanho_maximo=500
```

significa aproximadamente **500 caracteres**, não 500 palavras nem 500 tokens.

Estamos começando assim porque é simples de visualizar e depurar.

Mais adiante podemos melhorar para usar tokens e até overlap entre chunks, algo como:

```text
Chunk 1:
[A B C D E]

Chunk 2:
[D E F G H]
```

onde parte do conteúdo aparece nos dois chunks para preservar contexto.

Mas **não vamos colocar overlap agora**.

Quero primeiro provar que o chunking básico funciona.

Depois de fazer essas alterações, rode:

```powershell
python carregar_documentos.py
```

Você deverá continuar vendo:

```text
Documentos encontrados: 18
Com conteúdo: 18
Vazios: 0
Estrutura válida: 18
Com problemas de estrutura: 0
```

e agora também algo como:

```text
Total de chunks gerados: 40
```

O número **não precisa ser 40**. Pode ser 30, 50, 70 etc., dependendo do tamanho dos seus documentos.

Depois devem aparecer exemplos:

```text
----------------------------
Arquivo: inca_cancer.md
Chunk: 0
...

----------------------------
Arquivo: inca_cancer.md
Chunk: 1
...
```

Faça somente essa alteração agora e me mande a saída do terminal. Aí a gente verifica se os chunks estão ficando semanticamente bons antes de instalar o Sentence Transformers.


------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

Uhuuu! 🚀 **Chunking funcionando certinho.**

O resultado ficou muito bom:

```text
18 documentos
93 chunks
0 arquivos vazios
0 problemas de estrutura
```

Isso significa que, em média, cada documento virou cerca de 5 chunks. Para o tamanho da sua base, está bem razoável.

Os exemplos também mostram que a divisão preservou bem os parágrafos. Por exemplo, o `inca_cancer.md` foi separado em blocos de definição, fatores de risco, evidências e alegações relacionadas, sem cortar o texto no meio de uma palavra.

Então fechamos esta parte:

```text
[X] 8.2 Preparar documentos
[X] 8.3 Fazer chunking
[ ] 8.4 Instalar Sentence Transformers ← PRÓXIMO
[ ] 8.5 Escolher modelo de embeddings
[ ] 8.6 Gerar primeiro embedding
[ ] 8.7 Gerar embeddings dos 93 chunks
[ ] 8.8 Calcular similaridade de cosseno
[ ] 8.9 Fazer busca semântica
```

Agora vem a parte em que esses 93 textos vão virar vetores numéricos.

No terminal, dentro de `backend`, o próximo comando será:

```powershell
pip install sentence-transformers
```

Mas antes de rodar, um detalhe: essa instalação também traz dependências como `transformers`, `torch` e outras bibliotecas, então pode demorar alguns minutos e baixar bastante coisa.

Depois que terminar, **não vamos gerar os 93 embeddings de uma vez**. Primeiro faremos um teste simples com uma única frase para você ver exatamente o que o modelo devolve.


----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

Agora vamos escolher o modelo que vai transformar os **93 chunks** em vetores.

Para o nosso MVP, eu escolheria:

`sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2`

Ele foi desenvolvido para representar sentenças e parágrafos em um espaço vetorial de **384 dimensões** e é indicado para tarefas como **similaridade e busca semântica**. Além disso, é multilíngue, treinado para mais de 50 idiomas, o que faz sentido para nossa base em português. ([Hugging Face][1])

[Modelo no Hugging Face](https://huggingface.co/sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2?utm_source=chatgpt.com)

### Por que esse modelo?

Nosso objetivo não é gerar texto ainda. O modelo receberá:

```text
"Vacinação contra influenza reduz o risco de formas graves."
```

e produzirá:

```text
[0.037, -0.184, 0.292, ..., 0.081]
```

com **384 números**. ([Hugging Face][1])

Depois fazemos a mesma coisa com uma pesquisa:

```text
"A vacina da gripe ajuda a evitar casos graves?"
```

e comparamos os dois vetores.

O próprio Sentence Transformers lista esse MiniLM multilíngue entre seus modelos pré-treinados para mais de 50 idiomas. Existe também o `paraphrase-multilingual-mpnet-base-v2`, mas para nosso primeiro MVP eu prefiro começar com o MiniLM: queremos validar o pipeline com um modelo compacto antes de aumentar a complexidade. ([Sbert][2])

Então nossa decisão fica registrada:

```text
MODELO DE EMBEDDINGS

Nome:
sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2

Tipo:
Sentence Transformer multilíngue

Saída:
384 dimensões

Uso no HealthCheck:
Busca semântica

Idioma principal do projeto:
Português

Treinaremos do zero?
Não ❌

Executaremos localmente?
Sim ✅
```

Um detalhe importante: **você não precisa fazer outro `pip install` para esse modelo**. Já instalamos a biblioteca `sentence-transformers`. Na primeira vez que nosso Python executar:

```python
SentenceTransformer(
    "sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2"
)
```

a biblioteca fará o download dos arquivos do modelo; depois eles ficam em cache local para reutilização.

Portanto:

```text
[X] 8.4 Sentence Transformers instalado
[X] 8.5 Modelo escolhido ✅

[ ] 8.6 GERAR NOSSO PRIMEIRO EMBEDDING ← AGORA
```

No **8.6**, vamos criar um arquivo separado chamado `teste_embedding.py`. Não vamos mexer no `carregar_documentos.py` ainda. Primeiro quero que você coloque **uma frase em português**, execute o modelo e veja aparecer na tela o vetor de 384 números. Aí embeddings deixam de ser só teoria e você vê exatamente o que está acontecendo. 🚀

[1]: https://huggingface.co/sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2?utm_source=chatgpt.com "sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2 · Hugging Face"
[2]: https://sbert.net/docs/sentence_transformer/pretrained_models.html?utm_source=chatgpt.com "Pretrained Models — Sentence Transformers documentation"

------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

Bora. Agora sim vamos programar o **8.6 — primeiro embedding**.

Dentro da pasta `backend`, crie um arquivo novo chamado:

```text
teste_embedding.py
```

A estrutura fica assim:

```text
backend/
├── main.py
├── carregar_documentos.py
└── teste_embedding.py
```

Agora coloque dentro dele:

```python
from sentence_transformers import SentenceTransformer

modelo = SentenceTransformer(
    "sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2"
)

frase = "A vacina contra gripe ajuda a reduzir casos graves."

embedding = modelo.encode(frase)

print("Frase:")
print(frase)

print("\nEmbedding:")
print(embedding)

print("\nQuantidade de dimensões:")
print(len(embedding))
```

O que cada parte faz:

```python
from sentence_transformers import SentenceTransformer
```

Importa a classe que vai carregar nosso modelo de embeddings.

Depois:

```python
modelo = SentenceTransformer(
    "sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2"
)
```

Aqui estamos dizendo:

```text
use este modelo específico
```

Na primeira execução ele provavelmente vai baixar os arquivos do modelo.

Depois:

```python
frase = "A vacina contra gripe ajuda a reduzir casos graves."
```

Essa é nossa frase de teste.

Aqui:

```python
embedding = modelo.encode(frase)
```

acontece a parte principal:

```text
texto
  ↓
modelo
  ↓
vetor numérico
```

E finalmente:

```python
print(len(embedding))
```

serve para conferir quantas dimensões o vetor possui.

Agora salve o arquivo e, no terminal dentro de `backend`, rode:

```powershell
python teste_embedding.py
```

Na primeira vez pode aparecer download do modelo. Depois deve aparecer algo parecido com:

```text
Frase:
A vacina contra gripe ajuda a reduzir casos graves.

Embedding:
[ 0.021 ... -0.034 ... ]

Quantidade de dimensões:
384
```

Os números do vetor podem ser diferentes. O que queremos confirmar principalmente é:

```text
Quantidade de dimensões:
384
```

Se isso aparecer, o **primeiro embedding do HealthCheck IA foi gerado com sucesso**. 🚀

Me mande a saída do terminal depois que rodar.


------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------


UHUUU! 🚀 **Funcionou perfeitamente.**

Você acabou de gerar o **primeiro embedding real do HealthCheck IA**.

A frase:

```text
A vacina contra gripe ajuda a reduzir casos graves.
```

foi transformada em um vetor com:

```text
384 dimensões
```

e isso confirma que o modelo escolhido está funcionando corretamente.

Aquele aviso do Hugging Face sobre `HF_TOKEN` **não é erro**. Ele só informa que você está baixando sem autenticação; para esse teste e para o nosso MVP, podemos seguir normalmente.

Agora podemos marcar:

```text
[X] 8.5 Escolher modelo
[X] 8.6 Gerar primeiro embedding ✅

[ ] 8.7 Gerar embeddings dos 93 chunks  ← PRÓXIMO
[ ] 8.8 Similaridade de cosseno
[ ] 8.9 Primeira busca semântica
```

O próximo passo é transformar isso:

```text
93 chunks de texto
```

em:

```text
93 vetores
```

ou, tecnicamente, uma matriz aproximadamente assim:

```text
93 × 384
```

Ou seja:

```text
chunk 0 → 384 números
chunk 1 → 384 números
chunk 2 → 384 números
...
chunk 92 → 384 números
```

Mas não quero jogar esse código direto no seu `carregar_documentos.py` ainda. O ideal agora é criar uma separação melhor:

```text
carregar_documentos.py
→ carrega e cria chunks

teste_embedding.py
→ teste que já funcionou

busca_semantica.py
→ próximo arquivo, que vai gerar embeddings e depois pesquisar
```

Então o **8.6 está concluído com sucesso**. No próximo passo vamos criar juntos o `busca_semantica.py` e fazer ele pegar os 93 chunks automaticamente.


----------------------------------------------------------------------------------------------
----------------------------------------------------------------------------------------------
----------------------------------------------------------------------------------------------

UHUUU! 🚀 **Funcionou perfeitamente.**

Você acabou de gerar o **primeiro embedding real do HealthCheck IA**.

A frase:

```text
A vacina contra gripe ajuda a reduzir casos graves.
```

foi transformada em um vetor com:

```text
384 dimensões
```

e isso confirma que o modelo escolhido está funcionando corretamente.

Aquele aviso do Hugging Face sobre `HF_TOKEN` **não é erro**. Ele só informa que você está baixando sem autenticação; para esse teste e para o nosso MVP, podemos seguir normalmente.

Agora podemos marcar:

```text
[X] 8.5 Escolher modelo
[X] 8.6 Gerar primeiro embedding ✅

[ ] 8.7 Gerar embeddings dos 93 chunks  ← PRÓXIMO
[ ] 8.8 Similaridade de cosseno
[ ] 8.9 Primeira busca semântica
```

O próximo passo é transformar isso:

```text
93 chunks de texto
```

em:

```text
93 vetores
```

ou, tecnicamente, uma matriz aproximadamente assim:

```text
93 × 384
```

Ou seja:

```text
chunk 0 → 384 números
chunk 1 → 384 números
chunk 2 → 384 números
...
chunk 92 → 384 números
```

Mas não quero jogar esse código direto no seu `carregar_documentos.py` ainda. O ideal agora é criar uma separação melhor:

```text
carregar_documentos.py
→ carrega e cria chunks

teste_embedding.py
→ teste que já funcionou

busca_semantica.py
→ próximo arquivo, que vai gerar embeddings e depois pesquisar
```

Então o **8.6 está concluído com sucesso**. No próximo passo vamos criar juntos o `busca_semantica.py` e fazer ele pegar os 93 chunks automaticamente.


----------------------------------------------------------------------------------------------
----------------------------------------------------------------------------------------------
----------------------------------------------------------------------------------------------


Bora! 🚀 Agora começamos o **8.7 — gerar embeddings dos 93 chunks automaticamente**.

Por enquanto, nosso objetivo é somente chegar nisto:

```text
18 documentos
     ↓
93 chunks
     ↓
Sentence Transformer
     ↓
93 embeddings
     ↓
Matriz (93, 384)
```

Ainda **não vamos fazer a pesquisa semântica**. Primeiro vamos confirmar que conseguimos transformar todos os chunks em vetores.

### 1. Crie o novo arquivo

Dentro de `backend`, crie:

```text
busca_semantica.py
```

Sua estrutura ficará:

```text
backend/
├── main.py
├── carregar_documentos.py
├── teste_embedding.py
└── busca_semantica.py       ← NOVO
```

### 2. Comece importando o que precisamos

Dentro de `busca_semantica.py`, coloque:

```python
from sentence_transformers import SentenceTransformer
from carregar_documentos import chunks
```

A primeira linha:

```python
from sentence_transformers import SentenceTransformer
```

traz a ferramenta que gera embeddings.

A segunda:

```python
from carregar_documentos import chunks
```

é muito interessante.

Lembra que no outro arquivo criamos:

```python
chunks = []
```

e depois colocamos os 93 chunks dentro dessa lista?

Agora o `busca_semantica.py` está dizendo:

```text
carregar_documentos.py

possui:
chunks

        ↓

busca_semantica.py

quero usar esses chunks
```

Ou seja, **não precisamos carregar os 18 `.md` novamente manualmente**.

### 3. Carregue nosso modelo

Agora adicione:

```python
modelo = SentenceTransformer(
    "sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2"
)
```

É exatamente o modelo que testamos anteriormente.

Como você já o baixou no teste anterior, normalmente ele será carregado do cache do computador.

### 4. Pegue somente os textos dos chunks

Nossa variável `chunks` contém objetos assim:

```python
{
    "arquivo": "inca_cancer.md",
    "chunk_id": 0,
    "conteudo": "O câncer é..."
}
```

Mas o modelo precisa receber os textos.

Então coloque:

```python
textos = []

for chunk in chunks:
    textos.append(chunk["conteudo"])
```

Agora teremos:

```text
chunks
 ↓

[
 {arquivo, chunk_id, conteudo},
 {arquivo, chunk_id, conteudo},
 ...
]

 ↓

textos

[
 "texto do chunk 0",
 "texto do chunk 1",
 "texto do chunk 2",
 ...
]
```

Se temos 93 chunks:

```python
len(textos)
```

deverá ser:

```text
93
```

### 5. Agora vem a parte principal

Adicione:

```python
embeddings = modelo.encode(
    textos,
    show_progress_bar=True
)
```

Essa linha vai transformar **todos os 93 textos em vetores**.

Antes fizemos:

```python
modelo.encode(frase)
```

e produzimos:

```text
1 frase
↓
1 vetor de 384 dimensões
```

Agora estamos fazendo:

```text
93 frases/chunks
        ↓
modelo.encode()
        ↓
93 vetores
```

Portanto teremos matematicamente uma matriz:

```text
            384 dimensões

Chunk 0 → [.....................]
Chunk 1 → [.....................]
Chunk 2 → [.....................]
Chunk 3 → [.....................]
   ...
Chunk 92 → [....................]

          93 × 384
```

### 6. Vamos imprimir algumas informações

No final coloque:

```python
print("\n=== RESULTADO ===")

print(f"Quantidade de chunks: {len(chunks)}")
print(f"Quantidade de textos: {len(textos)}")

print(f"Formato dos embeddings: {embeddings.shape}")

print("\nPrimeiro chunk:")
print(chunks[0]["conteudo"][:300])

print("\nArquivo de origem:")
print(chunks[0]["arquivo"])

print("\nID do chunk:")
print(chunks[0]["chunk_id"])

print("\nPrimeiros 10 números do embedding:")
print(embeddings[0][:10])
```

### Seu `busca_semantica.py` completo

Por enquanto, ele deve ficar **exatamente assim**:

```python
from sentence_transformers import SentenceTransformer
from carregar_documentos import chunks


modelo = SentenceTransformer(
    "sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2"
)


textos = []

for chunk in chunks:
    textos.append(chunk["conteudo"])


embeddings = modelo.encode(
    textos,
    show_progress_bar=True
)


print("\n=== RESULTADO ===")

print(f"Quantidade de chunks: {len(chunks)}")
print(f"Quantidade de textos: {len(textos)}")

print(f"Formato dos embeddings: {embeddings.shape}")

print("\nPrimeiro chunk:")
print(chunks[0]["conteudo"][:300])

print("\nArquivo de origem:")
print(chunks[0]["arquivo"])

print("\nID do chunk:")
print(chunks[0]["chunk_id"])

print("\nPrimeiros 10 números do embedding:")
print(embeddings[0][:10])
```

### 7. Execute

Salve e rode:

```powershell
python busca_semantica.py
```

Vai aparecer uma barra parecida com:

```text
Batches: 100%|████████████████| ...
```

E no final queremos principalmente isto:

```text
=== RESULTADO ===

Quantidade de chunks: 93
Quantidade de textos: 93
Formato dos embeddings: (93, 384)
```

O `(93, 384)` é a confirmação mais importante.

Significa:

```text
93 linhas
×
384 números por linha
```

Ou seja:

```text
93 chunks
×
1 embedding de 384 dimensões para cada chunk
```

Você provavelmente também verá novamente os prints:

```text
Documentos encontrados: 18
...
Total de chunks gerados: 93
...
```

**Isso é esperado.** Acontece porque esta linha:

```python
from carregar_documentos import chunks
```

faz o Python executar o `carregar_documentos.py` para obter a variável `chunks`. Depois vamos organizar isso melhor.

Por enquanto, **não altere mais nada**.

Rode:

```powershell
python busca_semantica.py
```

e me mande a saída, principalmente a linha:

```text
Formato dos embeddings:
```

Se vier **`(93, 384)`**, teremos gerado os **93 embeddings do HealthCheck IA**. 🚀

------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------


UHUUU! 🚀 **Deu certo exatamente como queríamos.**

A confirmação principal é esta:

```text
Quantidade de chunks: 93
Quantidade de textos: 93
Formato dos embeddings: (93, 384)
```

Isso significa:

```text
93 chunks
↓
93 embeddings
↓
cada embedding tem 384 dimensões
```

Então, tecnicamente, agora você já tem uma matriz:

```text
93 x 384
```

Ou seja, a **base textual já foi convertida em representação vetorial**. Isso conclui o **8.7**. ✅

E aquelas linhas como:

```text
Primeiros 10 números do embedding:
[-0.10019718 -0.04441126 ...]
```

são só os primeiros valores do vetor do primeiro chunk. O restante existe também, totalizando 384 posições.

O aviso:

```text
Warning: You are sending unauthenticated requests to the HF Hub
```

continua não sendo problema. O modelo carregou normalmente e os embeddings foram gerados.

Agora nosso progresso está assim:

```text
[X] 8.2 Preparar documentos
[X] 8.3 Chunking
[X] 8.4 Sentence Transformers
[X] 8.5 Escolher modelo
[X] 8.6 Primeiro embedding
[X] 8.7 Embeddings dos 93 chunks ✅

[ ] 8.8 Similaridade de cosseno ← AGORA
[ ] 8.9 Primeira busca semântica
[ ] 8.10 Integrar ao backend
```

O próximo passo é o que vai fazer tudo começar a parecer uma IA de verdade: vamos escrever uma pergunta, por exemplo:

```text
"Vacina contra gripe reduz casos graves?"
```

transformar essa pergunta em embedding e comparar com os **93 vetores** usando **similaridade de cosseno**.

Aí o sistema vai nos devolver os chunks mais próximos semanticamente. Ou seja, começamos a sair de “vetores armazenados” para uma **busca semântica real**.

Se quiser, já podemos fazer agora o **8.8 — similaridade de cosseno**, mexendo no mesmo `busca_semantica.py`.


------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

Sim. Agora vamos para o **8.8 — similaridade de cosseno**, usando o mesmo `busca_semantica.py`.

A ideia agora é simples: você já tem os **93 embeddings dos chunks**. Vamos criar uma pergunta, gerar o embedding dela e comparar esse vetor com os 93 vetores da base.

No topo do arquivo, acrescente esta importação:

```python
from sklearn.metrics.pairwise import cosine_similarity
```

Depois, **logo após gerar os embeddings dos 93 textos**, acrescente:

```python
pergunta = "A vacina contra gripe ajuda a evitar casos graves?"

embedding_pergunta = modelo.encode([pergunta])

similaridades = cosine_similarity(
    embedding_pergunta,
    embeddings
)
```

Aqui acontece o seguinte:

```text
pergunta
↓
embedding de 384 dimensões
↓
comparação com os 93 embeddings
↓
93 valores de similaridade
```

Agora vamos descobrir qual chunk teve a maior similaridade. Acrescente:

```python
indice_mais_similar = similaridades[0].argmax()

maior_similaridade = similaridades[0][indice_mais_similar]
```

E depois:

```python
print("\n=== BUSCA POR SIMILARIDADE ===")

print("\nPergunta:")
print(pergunta)

print("\nMaior similaridade:")
print(maior_similaridade)

print("\nChunk mais parecido:")
print(chunks[indice_mais_similar]["conteudo"])

print("\nArquivo de origem:")
print(chunks[indice_mais_similar]["arquivo"])

print("\nID do chunk:")
print(chunks[indice_mais_similar]["chunk_id"])
```

Então seu arquivo passa a ter esta parte nova:

```python
from sentence_transformers import SentenceTransformer
from sklearn.metrics.pairwise import cosine_similarity
from carregar_documentos import chunks


modelo = SentenceTransformer(
    "sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2"
)


textos = []

for chunk in chunks:
    textos.append(chunk["conteudo"])


embeddings = modelo.encode(
    textos,
    show_progress_bar=True
)


pergunta = "A vacina contra gripe ajuda a evitar casos graves?"

embedding_pergunta = modelo.encode([pergunta])


similaridades = cosine_similarity(
    embedding_pergunta,
    embeddings
)


indice_mais_similar = similaridades[0].argmax()

maior_similaridade = similaridades[0][indice_mais_similar]


print("\n=== BUSCA POR SIMILARIDADE ===")

print("\nPergunta:")
print(pergunta)

print("\nMaior similaridade:")
print(maior_similaridade)

print("\nChunk mais parecido:")
print(chunks[indice_mais_similar]["conteudo"])

print("\nArquivo de origem:")
print(chunks[indice_mais_similar]["arquivo"])

print("\nID do chunk:")
print(chunks[indice_mais_similar]["chunk_id"])
```

Agora rode novamente:

```powershell
python busca_semantica.py
```

O valor da similaridade deverá ser algo entre aproximadamente:

```text
-1.0 até 1.0
```

Sendo que, para nosso caso:

```text
próximo de 1 → semanticamente muito parecido
próximo de 0 → pouca relação
negativo → direções semânticas pouco compatíveis
```

Por exemplo, pode aparecer algo como:

```text
Pergunta:
A vacina contra gripe ajuda a evitar casos graves?

Maior similaridade:
0.72
```

Mas **0,72 não significa “72% de verdade”**. Isso é muito importante.

Significa apenas que o vetor da pergunta e o vetor daquele chunk têm uma proximidade semântica de aproximadamente `0.72` pelo cosseno.

Matematicamente estamos calculando:

```text
cos(θ) = (A · B) / (||A|| × ||B||)
```

onde `A` é o embedding da pergunta e `B` é o embedding de cada chunk.

Um detalhe importante do código:

```python
embedding_pergunta = modelo.encode([pergunta])
```

Usamos:

```python
[pergunta]
```

em vez de apenas:

```python
pergunta
```

porque queremos que o resultado tenha formato:

```text
(1, 384)
```

Assim ele pode ser comparado diretamente com:

```text
(93, 384)
```

O resultado da comparação será:

```text
(1, 93)
```

Ou seja:

```text
pergunta
   ↓
[similaridade com chunk 0,
 similaridade com chunk 1,
 similaridade com chunk 2,
 ...
 similaridade com chunk 92]
```

E o:

```python
argmax()
```

simplesmente pergunta:

```text
qual posição possui o maior valor?
```

Essa posição corresponde ao chunk considerado semanticamente mais próximo.

Rode agora. O que eu quero ver principalmente é:

```text
Maior similaridade:
...

Chunk mais parecido:
...

Arquivo de origem:
...
```

Se ele encontrar algo relacionado a **vacina/gripe**, teremos acabado de fazer a primeira recuperação semântica real da base do HealthCheck IA. 🚀

------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------


Perfeito — **o 8.8 funcionou muito bem**. ✅

O ponto principal foi este:

```text
Maior similaridade:
0.8116439
```

E o sistema encontrou exatamente um chunk relevante sobre vacinação contra gripe:

```text
A vacina é uma das principais medidas de prevenção contra formas graves da gripe.
```

Isso mostra que a busca semântica está funcionando de verdade. Você perguntou:

```text
A vacina contra gripe ajuda a evitar casos graves?
```

e, mesmo sem a frase ser idêntica ao texto da base, o modelo encontrou o conteúdo semanticamente relacionado.

Então podemos marcar:

```text
[X] 8.7 Gerar embeddings dos 93 chunks
[X] 8.8 Similaridade de cosseno ✅

[ ] 8.9 Primeira busca semântica completa ← PRÓXIMO
```

Agora entra uma melhoria importante: hoje nós pegamos **apenas o chunk mais parecido** usando:

```python
argmax()
```

Mas em um sistema RAG de verdade, normalmente não queremos só 1 resultado. Queremos algo como os **3 ou 5 chunks mais relevantes**.

Por exemplo:

```text
Pergunta:
A vacina contra gripe ajuda a evitar casos graves?

TOP 3

1º — similaridade 0.81
ministerio_saude_gripe.md

2º — similaridade 0.74
fiocruz_vacinas.md

3º — similaridade 0.68
anvisa_vacinas.md
```

Isso é melhor porque a IA poderá depois analisar **várias evidências**, não apenas uma.

E repare numa coisa importante: `0.8116439` **não é 81% de chance de a afirmação ser verdadeira**. É somente o grau de proximidade entre os dois vetores segundo a similaridade de cosseno.

Seu pipeline agora já está realmente assim:

```text
Pergunta do usuário
       ↓
Embedding da pergunta
       ↓
[384 dimensões]
       ↓
Comparação com
93 embeddings da base
       ↓
Similaridade de cosseno
       ↓
Chunk mais relacionado
       ↓
ministerio_saude_gripe.md
```

Isso já é o **núcleo da recuperação de informação** que posteriormente vai alimentar o RAG.

No **8.9**, vamos alterar esse mesmo `busca_semantica.py` para retornar o **TOP 3 de evidências**, ordenadas da maior para a menor similaridade. Aí nossa busca semântica fica muito mais próxima do que usaremos no HealthCheck IA.


------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

Vamos. Agora o **8.9 — Primeira busca semântica completa** vai pegar a sua pergunta e retornar os **3 chunks mais relevantes**, em ordem de similaridade.

No `busca_semantica.py`, você pode **substituir a parte do `argmax()` para baixo** por isto:

```python
pergunta = "A vacina contra gripe ajuda a evitar casos graves?"

embedding_pergunta = modelo.encode([pergunta])

similaridades = cosine_similarity(
    embedding_pergunta,
    embeddings
)[0]

top_k = 3

indices_ordenados = similaridades.argsort()[::-1]

top_indices = indices_ordenados[:top_k]


print("\n=== BUSCA SEMÂNTICA ===")

print("\nPergunta:")
print(pergunta)

print(f"\nTOP {top_k} resultados mais relevantes:\n")

for posicao, indice in enumerate(top_indices, start=1):

    chunk = chunks[indice]
    similaridade = similaridades[indice]

    print("=" * 60)

    print(f"POSIÇÃO: {posicao}")
    print(f"SIMILARIDADE: {similaridade:.4f}")
    print(f"ARQUIVO: {chunk['arquivo']}")
    print(f"CHUNK ID: {chunk['chunk_id']}")

    print("\nCONTEÚDO:")
    print(chunk["conteudo"])

    print()
```

A parte nova mais importante é esta:

```python
indices_ordenados = similaridades.argsort()[::-1]
```

O `argsort()` pega as posições e ordena pelas similaridades. Só que ele ordena inicialmente do menor para o maior.

Por isso usamos:

```python
[::-1]
```

para inverter:

```text
maior
↓
menor
```

Depois:

```python
top_indices = indices_ordenados[:top_k]
```

significa:

```text
pegue somente os 3 primeiros
```

porque definimos:

```python
top_k = 3
```

Seu fluxo agora fica:

```text
Pergunta
   ↓
Embedding da pergunta
   ↓
384 dimensões
   ↓
Comparação com os 93 chunks
   ↓
93 similaridades
   ↓
Ordenação
   ↓
TOP 3
```

Seu `busca_semantica.py` completo pode ficar assim:

```python
from sentence_transformers import SentenceTransformer
from sklearn.metrics.pairwise import cosine_similarity
from carregar_documentos import chunks


modelo = SentenceTransformer(
    "sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2"
)


textos = []

for chunk in chunks:
    textos.append(chunk["conteudo"])


embeddings = modelo.encode(
    textos,
    show_progress_bar=True
)


pergunta = "A vacina contra gripe ajuda a evitar casos graves?"

embedding_pergunta = modelo.encode([pergunta])


similaridades = cosine_similarity(
    embedding_pergunta,
    embeddings
)[0]


top_k = 3

indices_ordenados = similaridades.argsort()[::-1]

top_indices = indices_ordenados[:top_k]


print("\n=== BUSCA SEMÂNTICA ===")

print("\nPergunta:")
print(pergunta)

print(f"\nTOP {top_k} resultados mais relevantes:\n")


for posicao, indice in enumerate(top_indices, start=1):

    chunk = chunks[indice]
    similaridade = similaridades[indice]

    print("=" * 60)

    print(f"POSIÇÃO: {posicao}")
    print(f"SIMILARIDADE: {similaridade:.4f}")
    print(f"ARQUIVO: {chunk['arquivo']}")
    print(f"CHUNK ID: {chunk['chunk_id']}")

    print("\nCONTEÚDO:")
    print(chunk["conteudo"])

    print()
```

Agora rode:

```powershell
python busca_semantica.py
```

Queremos algo parecido com:

```text
=== BUSCA SEMÂNTICA ===

Pergunta:
A vacina contra gripe ajuda a evitar casos graves?

TOP 3 resultados mais relevantes:

============================================================
POSIÇÃO: 1
SIMILARIDADE: 0.8116
ARQUIVO: ministerio_saude_gripe.md
CHUNK ID: 2

CONTEÚDO:
...

============================================================
POSIÇÃO: 2
SIMILARIDADE: 0.7...
ARQUIVO: ...
CHUNK ID: ...

============================================================
POSIÇÃO: 3
SIMILARIDADE: 0.6...
ARQUIVO: ...
CHUNK ID: ...
```

E aqui tem uma evolução conceitual importante: antes você fazia apenas:

```python
argmax()
```

ou seja:

```text
93 resultados
↓
escolha 1
```

Agora estamos fazendo:

```text
93 resultados
↓
ordene por relevância
↓
TOP 3
```

Isso já começa a formar o mecanismo de **retrieval** do futuro RAG:

```text
usuário
   ↓
pergunta
   ↓
busca semântica
   ↓
TOP 3 evidências
   ↓
[futuramente]
LLM
   ↓
análise + classificação + explicação
```

Rode esse código e me envie os **3 resultados**. O mais interessante agora será verificar se o modelo trouxe evidências coerentes em 1º, 2º e 3º lugar.


------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------


Perfeito. O **8.9 funcionou corretamente** e agora a busca semântica já está trazendo múltiplas evidências relevantes, em ordem de similaridade.

Repare como os três resultados fazem sentido para a pergunta. O 1º retornou diretamente que a vacina é uma das principais medidas contra formas graves da gripe, o 2º reforçou redução de agravamento, internações e óbitos, e o 3º trouxe uma evidência semelhante da Fiocruz. Isso é exatamente o comportamento que queríamos.

Seu fluxo agora está assim:

```text
Pergunta do usuário
        ↓
Embedding da pergunta
        ↓
Comparação com 93 embeddings
        ↓
Similaridade de cosseno
        ↓
Ordenação
        ↓
TOP 3 evidências
```

Então podemos marcar:

```text
[X] 8.7 Embeddings dos 93 chunks
[X] 8.8 Similaridade de cosseno
[X] 8.9 Primeira busca semântica completa ✅
```

Tem um ponto interessante no seu resultado: os dois primeiros chunks vieram do mesmo arquivo, `ministerio_saude_gripe.md`. Isso **não é um erro**. Como os dois trechos são semanticamente muito próximos da pergunta, eles ficaram no topo. Mais à frente podemos decidir se queremos diversidade de fontes, por exemplo limitar a no máximo 1 ou 2 chunks por documento.

O próximo passo lógico agora é transformar essa busca em uma **função reutilizável**, algo como:

```python
buscar_evidencias("vitamina C cura gripe?", top_k=3)
```

que devolva automaticamente os melhores chunks. Isso vai ser importante porque depois o FastAPI vai chamar essa função em vez de deixar a pergunta fixa dentro do arquivo.

Ou seja, o próximo passo é começar a sair de um script de teste e entrar numa estrutura que realmente possa alimentar o HealthCheck IA. 🚀


------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------


Sim, é isso. **O próximo passo natural é o 8.10 — integrar a busca semântica ao backend.**

Até agora, o `busca_semantica.py` funciona sozinho no terminal. Ele recebe uma pergunta fixa, gera o embedding, compara com os 93 chunks e retorna o TOP 3. Agora precisamos fazer o `main.py` conseguir chamar essa lógica quando a extensão enviar um título e uma descrição.

O fluxo vai passar de:

```text
pergunta fixa no busca_semantica.py
        ↓
TOP 3 evidências
```

para:

```text
Extensão do Chrome
        ↓
POST /analisar
        ↓
FastAPI
        ↓
buscar_evidencias()
        ↓
TOP 3 chunks
        ↓
resposta do backend
```

Mas eu faria uma pequena organização antes de ligar tudo diretamente. O `busca_semantica.py` precisa deixar de ser apenas um script de teste e virar algo reutilizável, por exemplo:

```python
def buscar_evidencias(pergunta, top_k=3):
    ...
    return resultados
```

Assim, no `main.py`, depois poderemos fazer algo como:

```python
from busca_semantica import buscar_evidencias
```

e:

```python
evidencias = buscar_evidencias(
    dados.titulo + " " + dados.descricao
)
```

Então sim, nossa sequência agora fica:

```text
[X] 8.7 Embeddings dos chunks
[X] 8.8 Similaridade de cosseno
[X] 8.9 Busca semântica TOP 3

[ ] 8.10 Integrar busca semântica ao backend ← AGORA
```

No **8.10**, eu sugiro fazermos em duas partes: primeiro transformar o `busca_semantica.py` em uma função reutilizável; depois modificar o `main.py` para usar essa função no endpoint `/analisar`.

Podemos começar agora pela **8.10.1 — transformar o `busca_semantica.py` em `buscar_evidencias(pergunta, top_k=3)`**, sem mexer ainda no FastAPI.


------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------


Sim. Vamos fazer exatamente isso: **8.10.1 — transformar o `busca_semantica.py` em uma função reutilizável**.

A ideia é que ele deixe de executar uma pergunta fixa automaticamente e passe a ter uma função que recebe qualquer pergunta.

Substitua o conteúdo atual do `busca_semantica.py` por este:

```python
from sentence_transformers import SentenceTransformer
from sklearn.metrics.pairwise import cosine_similarity
from carregar_documentos import chunks


modelo = SentenceTransformer(
    "sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2"
)


textos = []

for chunk in chunks:
    textos.append(chunk["conteudo"])


embeddings = modelo.encode(
    textos,
    show_progress_bar=True
)


def buscar_evidencias(pergunta, top_k=3):

    embedding_pergunta = modelo.encode([pergunta])

    similaridades = cosine_similarity(
        embedding_pergunta,
        embeddings
    )[0]

    indices_ordenados = similaridades.argsort()[::-1]

    top_indices = indices_ordenados[:top_k]

    resultados = []

    for indice in top_indices:

        chunk = chunks[indice]

        resultados.append({
            "arquivo": chunk["arquivo"],
            "chunk_id": chunk["chunk_id"],
            "conteudo": chunk["conteudo"],
            "similaridade": float(similaridades[indice])
        })

    return resultados


if __name__ == "__main__":

    pergunta = "A vacina contra gripe ajuda a evitar casos graves?"

    resultados = buscar_evidencias(
        pergunta,
        top_k=3
    )

    print("\n=== BUSCA SEMÂNTICA ===")

    print("\nPergunta:")
    print(pergunta)

    print("\nTOP 3 resultados mais relevantes:\n")

    for posicao, resultado in enumerate(resultados, start=1):

        print("=" * 60)

        print(f"POSIÇÃO: {posicao}")
        print(f"SIMILARIDADE: {resultado['similaridade']:.4f}")
        print(f"ARQUIVO: {resultado['arquivo']}")
        print(f"CHUNK ID: {resultado['chunk_id']}")

        print("\nCONTEÚDO:")
        print(resultado["conteudo"])

        print()
```

A parte mais importante agora é esta:

```python
def buscar_evidencias(pergunta, top_k=3):
```

Agora você criou uma função que pode receber qualquer texto.

Por exemplo:

```python
buscar_evidencias(
    "Vitamina C cura gripe?"
)
```

ou:

```python
buscar_evidencias(
    "Vacinas causam doenças?"
)
```

ou depois, no FastAPI:

```python
buscar_evidencias(
    dados.titulo + " " + dados.descricao
)
```

O `top_k=3` significa que, se você não informar nada, ela retorna os 3 resultados mais relevantes.

Mas você também poderia fazer:

```python
buscar_evidencias(
    "Vacina contra gripe funciona?",
    top_k=5
)
```

e receber os 5 melhores.

Uma mudança importante também aconteceu aqui:

```python
resultados = []
```

Antes nosso programa simplesmente imprimia tudo.

Agora ele cria uma estrutura de dados assim:

```python
[
    {
        "arquivo": "ministerio_saude_gripe.md",
        "chunk_id": 2,
        "conteudo": "...",
        "similaridade": 0.8116
    },
    {
        "arquivo": "...",
        "chunk_id": 1,
        "conteudo": "...",
        "similaridade": 0.7868
    },
    ...
]
```

Isso é muito melhor porque o FastAPI poderá receber esses dados e transformá-los em JSON.

Também quero que você entenda esta parte:

```python
if __name__ == "__main__":
```

Ela serve para separar dois comportamentos.

Quando você executar:

```powershell
python busca_semantica.py
```

o código de teste será executado normalmente.

Mas quando o futuro `main.py` fizer:

```python
from busca_semantica import buscar_evidencias
```

essa parte de teste:

```python
pergunta = "A vacina contra gripe..."
```

**não será executada**.

Isso é exatamente o que precisamos para reutilizar o arquivo dentro do backend.

Agora rode novamente:

```powershell
python busca_semantica.py
```

O resultado deverá continuar praticamente igual ao anterior, com o TOP 3.

Se continuar retornando algo como:

```text
1 - ministerio_saude_gripe.md
2 - ministerio_saude_gripe.md
3 - fiocruz_vacinas.md
```

então teremos confirmado que a refatoração funcionou.

Nesse ponto:

```text
[X] 8.10.1 Criar buscar_evidencias()
[ ] 8.10.2 Conectar buscar_evidencias() ao FastAPI
```

E no **8.10.2** finalmente vamos mexer no `main.py`, fazendo o `/analisar` usar a busca semântica real em vez de responder sempre:

```json
{
  "classificacao": "enganosa",
  "confianca": 0.82
}
```

Esse próximo passo vai ser importante porque será a primeira vez que a **extensão → FastAPI → embeddings → base de saúde** vão começar a funcionar como um único sistema.


------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------


Perfeito. Agora vamos fazer o **8.10.2 — conectar `buscar_evidencias()` ao FastAPI**.

A ideia é que o endpoint `/analisar` deixe de responder algo fixo e passe a usar o texto recebido da extensão para buscar evidências reais na sua base.

No `main.py`, adicione esta importação:

```python
from busca_semantica import buscar_evidencias
```

Depois, vamos mudar os modelos de resposta para incluir as evidências. Seu `main.py` pode ficar assim:

```python
from fastapi import FastAPI
from pydantic import BaseModel
from fastapi.middleware.cors import CORSMiddleware

from busca_semantica import buscar_evidencias


app = FastAPI(
    title="HealthCheck IA API",
    version="1.0.0"
)


app.add_middleware(
    CORSMiddleware,
    allow_origins=["*"],
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)


class AnaliseRequest(BaseModel):
    titulo: str
    descricao: str


class Evidencia(BaseModel):
    arquivo: str
    chunk_id: int
    conteudo: str
    similaridade: float


class AnaliseResponse(BaseModel):
    evidencias: list[Evidencia]


@app.get("/")
def raiz():
    return {
        "mensagem": "HealthCheck IA API funcionando"
    }


@app.post(
    "/analisar",
    response_model=AnaliseResponse
)
def analisar(dados: AnaliseRequest):

    texto_para_analisar = (
        dados.titulo
        + " "
        + dados.descricao
    )

    evidencias = buscar_evidencias(
        texto_para_analisar,
        top_k=3
    )

    return AnaliseResponse(
        evidencias=evidencias
    )
```

O coração da integração está aqui:

```python
texto_para_analisar = (
    dados.titulo
    + " "
    + dados.descricao
)
```

Sua extensão já manda algo parecido com:

```json
{
  "titulo": "Vacina contra gripe reduz casos graves",
  "descricao": "Veja como a vacinação pode reduzir internações e complicações."
}
```

Agora o backend junta isso:

```text
Vacina contra gripe reduz casos graves Veja como a vacinação pode reduzir internações e complicações.
```

Depois essa parte:

```python
evidencias = buscar_evidencias(
    texto_para_analisar,
    top_k=3
)
```

faz toda a cadeia que construímos:

```text
título + descrição
       ↓
embedding
       ↓
comparação com 93 chunks
       ↓
similaridade de cosseno
       ↓
TOP 3
```

E o FastAPI vai devolver algo parecido com:

```json
{
  "evidencias": [
    {
      "arquivo": "ministerio_saude_gripe.md",
      "chunk_id": 2,
      "conteudo": "A vacinação contra influenza...",
      "similaridade": 0.81
    },
    {
      "arquivo": "ministerio_saude_gripe.md",
      "chunk_id": 1,
      "conteudo": "A vacinação tem como objetivo...",
      "similaridade": 0.78
    },
    {
      "arquivo": "fiocruz_vacinas.md",
      "chunk_id": 2,
      "conteudo": "A vacinação é uma das principais...",
      "similaridade": 0.78
    }
  ]
}
```

## Agora vamos testar sem mexer na extensão ainda

Salve o `main.py`.

Depois pare o servidor, caso esteja aberto, usando:

```powershell
CTRL + C
```

E inicie novamente:

```powershell
uvicorn main:app --reload
```

Abra:

```text
http://127.0.0.1:8000/docs
```

Entre em:

```text
POST /analisar
```

Clique em **Try it out** e teste:

```json
{
  "titulo": "Vacina contra gripe ajuda a evitar casos graves?",
  "descricao": "A vacinação pode reduzir internações e mortes por influenza."
}
```

Depois clique em:

```text
Execute
```

O que queremos ver agora é:

```json
{
  "evidencias": [
    ...
  ]
}
```

com três resultados reais da nossa base.

### Uma coisa importante pode acontecer

Quando você iniciar:

```powershell
uvicorn main:app --reload
```

é provável que apareçam novamente:

```text
Documentos encontrados: 18
Total de chunks gerados: 93
Loading weights...
Batches...
```

Isso acontece porque:

```text
main.py
↓
importa busca_semantica.py
↓
busca_semantica.py importa carregar_documentos.py
↓
carrega 18 documentos
↓
gera 93 chunks
↓
carrega o modelo
↓
gera 93 embeddings
```

Isso é esperado **neste momento**.

Inclusive, há uma vantagem: o modelo e os embeddings ficam carregados quando a aplicação inicia. Quando chegar uma requisição, não precisamos gerar os 93 embeddings novamente a cada chamada.

Nosso sistema passa a ter esta arquitetura:

```text
Chrome Extension
      ↓
título + descrição
      ↓
POST /analisar
      ↓
FastAPI
      ↓
buscar_evidencias()
      ↓
Sentence Transformer
      ↓
Embedding da alegação
      ↓
93 embeddings da base
      ↓
Cosine Similarity
      ↓
TOP 3 evidências
      ↓
JSON
```

Mas ainda falta uma peça importante: **isso não classifica a notícia**.

Neste momento o backend sabe dizer:

> “Estes são os três trechos da nossa base semanticamente mais relacionados com aquilo que encontrei no Google.”

Ele ainda **não sabe dizer** se a alegação é sustentada, enganosa ou contradita pelas evidências.

Essa inteligência entra depois, quando passarmos as evidências recuperadas para o componente de análise/RAG/LLM.

Primeiro faça esse teste pelo Swagger. Se aparecerem as três evidências no `Response body`, o **8.10.2 estará funcionando** e teremos conectado pela primeira vez **FastAPI + embeddings + busca semântica + base de saúde**. 🚀


------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

