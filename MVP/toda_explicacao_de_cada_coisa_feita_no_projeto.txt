“Desenvolver uma solução baseada em Inteligência Artificial para auxiliar na identificação e verificação de desinformação relacionada à saúde, apresentando evidências e fontes confiáveis sem substituir o pensamento crítico do usuário.”

Fazer o projeto só com ferramentas gratuitas. Arquitetura 100% gratuita assim:

**Usuário → Streamlit → Python → busca de evidências → IA local → classificação/explicação → fontes**

| Parte | Ferramenta gratuita |
| --- | --- |
| Linguagem | Python |
| Interface/site | Streamlit |
| Manipulação de dados | Pandas |
| Machine Learning | Scikit-learn |
| IA generativa | Ollama + modelo open-source |
| Embeddings | Sentence Transformers |
| Banco vetorial/RAG | FAISS |
| Banco tradicional | SQLite |
| APIs/backend, se necessário | FastAPI |
| Código/versionamento | Git + GitHub |
| Experimentos | MLflow |
| Desenvolvimento | VS Code/Jupyter |
| Protótipo visual | Figma gratuito |

O Streamlit, por exemplo, é open source e permite construir a interface diretamente em Python. A própria documentação mostra como criar interfaces de chatbot e aplicações de IA. (Streamlit Docs)

### Como ficaria seu projeto

Imagine que alguém recebe no WhatsApp:

> **"Tomar determinada substância cura diabetes."**
> 

A pessoa cola essa informação no seu sistema.

Seu programa poderia executar:

**1. Identificar a alegação**

→ "Substância X cura diabetes."

**2. Procurar evidências**

O sistema consulta uma base que vocês montaram com conteúdos provenientes de fontes confiáveis, como órgãos de saúde e literatura científica.

**3. Recuperar as evidências mais relacionadas**

Aqui você pode implementar **RAG** gratuitamente com embeddings locais + FAISS.

**4. IA analisa as evidências**

Você não precisa necessariamente da API da OpenAI. Pode rodar um modelo open-source **localmente pelo Ollama**.

**5. O sistema apresenta algo como:**

> **🟠 Contradita pelas evidências**
> 
> 
> Foram encontradas evidências que contradizem essa alegação.
> 
> **Evidências encontradas**
> 
> Ministério da Saúde — [...]
> 
> Fiocruz — [...]
> 
> Artigo científico — [...]
> 
> **Por que merece atenção?**
> 
> A publicação afirma existir uma "cura", mas as evidências consultadas não sustentam essa conclusão.
> 
> **Analise as fontes abaixo antes de compartilhar.**
> 

Isso está muito alinhado ao documento de vocês, que propõe justamente categorias como **Sustentada, Parcialmente sustentada, Enganosa, Contradita pelas evidências e Não foi possível verificar**.

### E tem uma vantagem importante

Você **não precisa treinar uma IA gigantesca do zero**.

Isso tornaria o projeto caro e desnecessariamente complicado.

Para um MVP, eu seguiria:

**LLM local + RAG + base confiável de evidências.**

O sistema recupera documentos relevantes e utiliza o modelo para **analisar a alegação em relação às evidências recuperadas**, em vez de esperar que o modelo "saiba sozinho" se a notícia é verdadeira.

E isso combina muito bem com a própria investigação prevista no trabalho, que manda comparar **ML tradicional, modelo especializado, LLM e RAG + LLM**.

### Inclusive seu computador consegue ajudar bastante

Como você já tem uma **RTX 2060 6 GB + 32 GB RAM**, dá para experimentar modelos locais relativamente pequenos sem depender de API paga. Não significa que qualquer LLM vai rodar bem — modelos grandes serão pesados —, mas modelos menores/quantizados são suficientes para experimentar um MVP desse tipo.

Eu estruturaria o projeto em **6 etapas**:

1. **Dataset** — conseguir exemplos de desinformação relacionada à saúde.
2. **Fontes confiáveis** — montar a base de evidências.
3. **RAG** — recuperar evidências relacionadas à afirmação.
4. **LLM local** — analisar alegação + evidências.
5. **Streamlit** — criar a interface.
6. **MLflow/testes** — avaliar se o sistema está acertando e documentar os experimentos.

E nem a hospedagem precisa necessariamente custar. A documentação do Streamlit atualmente mantém uma opção de **Community Cloud gratuita** para compartilhar aplicações. (Streamlit Docs)

**Então sim: eu considero perfeitamente viável vocês entregarem um MVP funcional sem gastar dinheiro com software.**

O cuidado principal é não tentar fazer um "detector universal de fake news". **Restrinjam o MVP à saúde, texto e um conjunto controlado de fontes/evidências.** Isso deixa o projeto realizável e, tecnicamente, muito mais defensável.

# Entanção

Sim, **tem como fazer** — e para um MVP acadêmico dá para fazer praticamente tudo com ferramentas gratuitas.

A ideia da extensão seria esta: quando o usuário fizer uma busca no Google, a extensão lê os resultados exibidos na página e injeta, logo abaixo de cada resultado, uma análise como “Baixa confiabilidade”, “Evidências fortes”, “Fonte duvidosa” ou até uma porcentagem estimada. Extensões do Chrome conseguem injetar JavaScript em páginas e modificar o conteúdo exibido, então tecnicamente essa parte é viável. (Chrome for Developers)

Mas eu mudaria uma coisa importante no seu conceito: **não usaria “30% de chance de ser verdade” ou “80% de chance de ser verdade” como se fosse uma probabilidade objetiva**, porque isso é muito difícil de justificar cientificamente. Para o projeto de vocês, seria mais seguro e tecnicamente defensável mostrar algo como:

- **Evidências fortes a favor**
- **Parcialmente sustentada**
- **Evidências contraditórias**
- **Fonte com baixa confiabilidade**
- **Não foi possível verificar**
- **Confiança da análise: 82%**

Essa última porcentagem seria a **confiança do modelo na própria classificação**, e não “82% de chance da notícia ser verdadeira”. Isso combina muito melhor com o projeto do documento, que já propõe categorias como “Sustentada”, “Parcialmente sustentada”, “Enganosa”, “Contradita pelas evidências” e “Não foi possível verificar”.

A arquitetura poderia ser totalmente gratuita:

**Google → Extensão Chrome → lê título/snippet/link → envia para um backend local → RAG/IA analisa → devolve classificação → extensão mostra abaixo do resultado**

Você poderia usar **JavaScript + Manifest V3** na extensão, **Python + FastAPI** no backend, **FAISS** para busca vetorial, **Sentence Transformers** para embeddings, **Ollama + modelo open-source** para análise e **SQLite** para guardar dados. Tudo isso pode ser usado sem pagar licença.

O fluxo ficaria mais ou menos assim:

> Pesquisa: “chá X cura câncer”
> 

A página do Google mostra vários resultados.

A extensão captura cada título, trecho e URL.

Depois consulta sua IA.

E abaixo de um resultado apareceria algo como:

> 🟠 **Evidências contraditórias**
> 
> 
> Confiança da análise: 87%
> 
> A alegação não é sustentada pelas fontes científicas consultadas.
> 
> **Fontes verificadas:** Fiocruz, Ministério da Saúde, PubMed
> 
> “Ver evidências”
> 

Isso seria, inclusive, **um produto muito mais interessante do que simplesmente criar um site onde a pessoa cola uma notícia**, porque a ferramenta entra exatamente no momento em que o usuário está consumindo informação.

O principal desafio não é fazer a extensão. **A extensão é relativamente simples. O difícil é construir um sistema confiável de avaliação.** Você vai precisar definir como a IA decide a classificação, quais fontes são consideradas confiáveis, como recuperar evidências e o que fazer quando não existir evidência suficiente. Isso também está exatamente dentro das perguntas de pesquisa previstas no projeto.

Para não deixar o projeto grande demais, eu faria o **MVP somente para buscas de saúde e somente texto**. Nada de tentar analisar qualquer assunto da internet, imagem, vídeo, política etc. inicialmente.

Por exemplo:

**Versão 1 do projeto**

**Extensão Chrome de verificação de informações sobre saúde**

Ela analisaria:

**título + descrição + domínio do resultado + evidências recuperadas de uma base confiável.**

E poderia ter três componentes:

**1. Extensão Chrome**

Mostra a classificação diretamente no Google.

**2. Motor de IA**

Analisa as alegações.

**3. Base de evidências**

Ministério da Saúde, Anvisa, Fiocruz, PubMed e outras fontes previamente selecionadas.

E existe outro ponto interessante: vocês podem instalar a extensão localmente durante o desenvolvimento, sem precisar publicá-la na Chrome Web Store. A documentação oficial do Chrome oferece suporte e exemplos para desenvolvimento e carregamento de extensões. (Chrome for Developers)

Então, para a faculdade, vocês poderiam chegar na apresentação, abrir o Chrome, pesquisar:

> **“vacina da gripe causa gripe?”**
> 

e os resultados aparecerem já com as avaliações da IA embaixo.

Isso teria **muito mais impacto visual na apresentação** do que simplesmente mostrar um notebook executando um modelo.

E o mais importante: **sim, é um projeto que dá para construir.** Eu só evitaria vender a ideia como “IA que sabe a porcentagem de uma notícia ser verdadeira”. Eu apresentaria como:

> **“Uma extensão inteligente para Chrome que analisa resultados de busca relacionados à saúde, cruza as alegações com fontes confiáveis e apresenta ao usuário evidências e um nível de confiança antes que ele acesse ou compartilhe o conteúdo.”**
> 

Essa formulação é tecnicamente mais forte e está bem alinhada ao objetivo do trabalho.

Projeto IA Fake  News

----------------------------------------------------------------------------------------------------------
-

Perfeito. Vamos transformar isso em um **roteiro de desenvolvimento**, para você sempre saber em que etapa está, o que já terminou e qual é o próximo passo.

O projeto final será:

> **Uma extensão do Chrome que analisa resultados de pesquisas de saúde no Google e mostra, embaixo de cada resultado, uma classificação de confiabilidade baseada em evidências e fontes confiáveis.**

Vou dividir em **12 etapas**.

| Etapa | Objetivo                     | Resultado esperado                                           |
| ----- | ---------------------------- | ------------------------------------------------------------ |
| 1     | Definir o MVP                | Saber exatamente o que o sistema fará e o que ficará de fora |
| 2     | Criar extensão Chrome básica | Extensão instalada e funcionando                             |
| 3     | Ler resultados do Google     | Capturar título, descrição e URL                             |
| 4     | Inserir análise visual       | Mostrar um selo embaixo de cada resultado                    |
| 5     | Criar backend                | API em Python/FastAPI funcionando                            |
| 6     | Conectar extensão + backend  | Extensão envia resultados e recebe resposta                  |
| 7     | Criar base de evidências     | Ter documentos confiáveis de saúde                           |
| 8     | Criar busca semântica        | Encontrar evidências relacionadas à pesquisa                 |
| 9     | Integrar IA                  | IA analisa alegação + evidências                             |
| 10    | Criar classificação          | Definir resultado e nível de confiança                       |
| 11    | Testar e avaliar             | Medir erros, acertos e limitações                            |
| 12    | Preparar apresentação        | README, arquitetura, vídeo/demo e documentação               |

## ETAPA 1 — Definir exatamente o MVP

Antes de programar, vamos especificar o produto.

O **MVP 1.0** deve fazer somente isto:

1. usuário pesquisa alguma coisa no Google;
2. extensão detecta os resultados;
3. captura título, descrição e URL;
4. analisa somente conteúdos relacionados à saúde;
5. consulta nossa base de evidências;
6. apresenta uma classificação;
7. mostra as fontes/evidências utilizadas.

Exemplo:

> 🔴 **Contradita pelas evidências**
> Confiança da análise: 87%
> Encontramos evidências que contradizem essa afirmação.
> Fontes: Ministério da Saúde, Fiocruz e PubMed.

E vamos deixar **fora da versão inicial**:

* vídeos;
* imagens;
* Instagram;
* WhatsApp;
* política;
* notícias esportivas;
* qualquer idioma além de português;
* análise da internet inteira.

Isso evita o maior risco do projeto: tentar fazer coisa demais.

---

# ETAPA 2 — Extensão Chrome básica

Aqui **não existe IA ainda**.

Você vai estudar:

* HTML;
* CSS;
* JavaScript básico;
* `manifest.json`;
* Chrome Extensions;
* Content Scripts;
* DOM.

Primeiro objetivo:

Você pesquisa alguma coisa no Google e aparece:

> 🔎 HealthCheck IA — aguardando análise

embaixo de cada resultado.

Quando isso funcionar, considero a Etapa 2 concluída.

---

# ETAPA 3 — Capturar os resultados do Google

Agora a extensão precisa entender a página.

Ela deverá capturar algo assim:

```javascript
{
    titulo: "Vitamina C cura gripe?",
    descricao: "Veja o que dizem os estudos...",
    url: "https://exemplo.com/artigo"
}
```

Você vai estudar:

* DOM;
* `querySelector()`;
* `querySelectorAll()`;
* arrays;
* objetos JavaScript;
* manipulação de elementos HTML.

Meta:

```text
Resultado 1
Título: ...
URL: ...
Descrição: ...

Resultado 2
Título: ...
URL: ...
Descrição: ...
```

aparecendo no console.

---

# ETAPA 4 — Criar o componente visual

Agora vamos criar o visual que aparecerá no Google.

Inicialmente será falso/simulado:

```text
🟢 Evidências favoráveis

Confiança: 85%

Ver análise
```

ou:

```text
🔴 Evidências contraditórias

Confiança: 82%

Ver evidências
```

Aqui entra:

* CSS;
* componentes;
* badges;
* botões;
* expansão/recolhimento;
* acessibilidade básica.

Nesse momento a extensão já vai **parecer um produto real**, mesmo sem IA.

---

# ETAPA 5 — Backend Python

Agora criamos o servidor responsável pela inteligência.

Tecnologia:

**Python + FastAPI**

Você vai estudar:

* API;
* REST;
* HTTP;
* GET;
* POST;
* JSON;
* request;
* response;
* FastAPI;
* Pydantic;
* CORS.

Teremos algo como:

```text
POST /analisar
```

Entrada:

```json
{
  "titulo": "Vitamina C cura gripe?",
  "descricao": "..."
}
```

Resposta inicialmente simulada:

```json
{
  "classificacao": "enganosa",
  "confianca": 0.82
}
```

Essa etapa é importantíssima porque separa:

```text
EXTENSÃO
```

de:

```text
INTELIGÊNCIA DO SISTEMA
```

---

# ETAPA 6 — Conectar Chrome com Python

Aqui os dois projetos se encontram.

Fluxo:

```text
Google
 ↓
Extensão
 ↓
JavaScript
 ↓
HTTP POST
 ↓
FastAPI
 ↓
JSON
 ↓
Extensão
 ↓
Google
```

Vamos utilizar JavaScript:

```javascript
fetch(...)
```

A extensão manda a notícia para nosso backend.

E recebe:

```json
{
    "classificacao": "contradita",
    "confianca": 0.91
}
```

Então muda automaticamente o selo.

Quando isso acontecer, teremos uma arquitetura cliente-servidor funcionando.

---

# ETAPA 7 — Base de conhecimento em saúde

Aqui começa a parte científica.

Precisaremos construir uma base confiável.

Seu próprio projeto menciona referências como:

* Ministério da Saúde;
* Anvisa;
* Fiocruz;
* SUS;
* BVS;
* PubMed;
* universidades;
* sociedades médicas. 

Inicialmente eu não tentaria conectar tudo automaticamente.

Pegamos um conjunto pequeno de documentos confiáveis.

Algo como:

```text
data/

vacinas/
diabetes/
hipertensao/
cancer/
dengue/
medicamentos/
```

E colocamos documentos dentro.

---

# ETAPA 8 — Embeddings + busca semântica

Essa é uma etapa fundamental.

Imagine que sua base possui:

> “Não existem evidências científicas de que vitamina C cure infecções por influenza.”

E o usuário pesquisa:

> “Tomar vitamina C acaba com a gripe?”

As frases não são iguais.

Mas semanticamente estão relacionadas.

Para encontrar isso vamos estudar:

* NLP;
* embeddings;
* vetores;
* similaridade de cosseno;
* Sentence Transformers;
* chunking;
* busca vetorial.

A arquitetura será:

```text
Pesquisa
   ↓
Embedding
   ↓
Busca vetorial
   ↓
Documentos semelhantes
```

---

# ETAPA 9 — Construir o RAG

Depois juntamos:

**Retrieval + IA.**

O fluxo será:

```text
Alegação
 ↓
Busca semântica
 ↓
Encontrar 3-5 evidências
 ↓
LLM
 ↓
Análise
```

A IA receberá algo parecido com:

```text
ALEGAÇÃO:

"Vitamina C cura gripe."

EVIDÊNCIA 1:
...

EVIDÊNCIA 2:
...

EVIDÊNCIA 3:
...

Utilizando SOMENTE essas evidências,
avalie a alegação.
```

Isso reduz bastante o risco de a IA inventar informações.

---

# ETAPA 10 — Integrar o LLM

Para tentar manter tudo gratuito, podemos experimentar:

**Ollama + modelo open-source.**

A IA não precisa saber tudo.

Ela receberá as evidências fornecidas pelo RAG.

Estudaremos:

* LLM;
* contexto;
* prompt;
* system prompt;
* temperatura;
* hallucination;
* structured output;
* JSON Schema.

Queremos obrigar a IA a retornar algo assim:

```json
{
    "classificacao": "contradita",
    "confianca": 0.91,
    "explicacao": "As evidências encontradas...",
    "fontes": [
        "Fiocruz",
        "Ministério da Saúde"
    ]
}
```

---

# ETAPA 11 — Sistema de classificação

Vamos padronizar as possíveis respostas.

Eu sugiro usar as categorias já previstas pelo projeto:

```text
🟢 SUSTENTADA

🟡 PARCIALMENTE SUSTENTADA

🟠 ENGANOSA

🔴 CONTRADITA PELAS EVIDÊNCIAS

⚪ NÃO FOI POSSÍVEL VERIFICAR
```

Essas categorias estão presentes no planejamento original. 

Isso é melhor que:

> 25% verdadeira

porque uma porcentagem dessas pode parecer cientificamente precisa sem realmente ser.

Podemos ter:

> **Classificação:** contradita pelas evidências
> **Confiança do sistema:** 86%

---

# ETAPA 12 — Avaliação científica

Aqui você transforma um programa interessante em um **projeto acadêmico sério**.

Criaremos um dataset de teste.

Por exemplo:

```text
200 alegações
```

Cada uma já terá um resultado correto conhecido.

Depois:

```text
Sistema analisa
       ↓
comparamos
       ↓
resultado esperado
       X
resultado produzido
```

Vamos calcular:

* accuracy;
* precision;
* recall;
* F1-score;
* matriz de confusão.

Também podemos utilizar **MLflow** para registrar os experimentos.

Você vai conseguir dizer na apresentação:

> “Nosso modelo foi avaliado em 200 alegações verificadas e atingiu F1-score de X.”

Isso é muito mais forte do que apenas:

> “Olha, nossa IA funciona.”

---

# ETAPA 13 — Pensamento crítico

Essa parte precisa existir no produto.

Seu documento deixa claro que uma das questões centrais é evitar que a IA substitua a capacidade de julgamento da pessoa. 

Então não mostraremos apenas:

> ❌ FAKE

Mostraremos:

> 🔴 **Evidências contraditórias**
>
> Encontramos três fontes que apresentam evidências diferentes da afirmação.
>
> **Veja por quê**
>
> [...]
>
> **Consulte as fontes**
>
> [...]
>
> Antes de compartilhar, compare as evidências apresentadas.

A IA vira uma ferramenta de **apoio ao raciocínio**, não uma autoridade absoluta.

---

# ETAPA 14 — Segurança e limitações

Precisaremos estabelecer regras.

Por exemplo:

Se a evidência for insuficiente:

> ⚪ **Não foi possível verificar**

Nunca deixar a IA inventar uma resposta.

Também precisamos documentar:

* limitações;
* possíveis vieses;
* atualização da base;
* falsos positivos;
* falsos negativos;
* uso responsável.

O próprio planejamento prevê uma política de **abstenção da IA quando as evidências forem insuficientes ou contraditórias**. 

---

# ETAPA 15 — GitHub e documentação

Depois vamos profissionalizar.

Estrutura aproximada:

```text
healthcheck-ai/

extension/
backend/
ai/
data/
tests/
docs/
README.md
requirements.txt
.gitignore
```

E vamos trabalhar com commits organizados:

```text
feat: create chrome extension structure

feat: capture google search results

feat: create analysis api

feat: integrate extension with api

feat: implement semantic retrieval

feat: integrate local llm

test: add classification evaluation
```

---

# ETAPA 16 — Apresentação

No final teremos uma demonstração extremamente simples.

Você abre o Chrome.

Pesquisa:

```text
vacina da gripe causa gripe
```

Google mostra os resultados.

Alguns segundos depois:

```text
🔴 Evidências contraditórias
Confiança: 91%

3 evidências encontradas

[Ver evidências]
```

Clica.

Aparecem as fontes.

Depois você explica:

```text
Chrome Extension
       ↓
FastAPI
       ↓
RAG
       ↓
Base científica
       ↓
LLM
       ↓
Resposta explicável
```

E fecha apresentando as métricas dos testes.

---

## Nosso mapa oficial

Para você não se perder, vamos usar este acompanhamento:

```text
PROJETO — HEALTHCHECK IA

[ ] 01 — Definir MVP
[ ] 02 — Criar extensão Chrome
[ ] 03 — Capturar resultados Google
[ ] 04 — Criar interface dos selos
[ ] 05 — Criar FastAPI
[ ] 06 — Conectar extensão + API
[ ] 07 — Criar base de saúde
[ ] 08 — Implementar embeddings
[ ] 09 — Implementar busca vetorial
[ ] 10 — Implementar RAG
[ ] 11 — Integrar LLM local
[ ] 12 — Criar classificação
[ ] 13 — Mostrar evidências/fontes
[ ] 14 — Implementar pensamento crítico
[ ] 15 — Criar testes
[ ] 16 — Avaliar métricas
[ ] 17 — Integrar MLflow
[ ] 18 — Documentar limitações
[ ] 19 — Organizar GitHub
[ ] 20 — Preparar demonstração
[ ] 21 — Preparar apresentação
```

E tem uma regra que quero que a gente siga durante todo o desenvolvimento:

> **Não avançamos para a próxima camada até a anterior estar funcionando.**

Então nosso **primeiro objetivo real é muito pequeno**:

**criar uma extensão Chrome local que consiga alterar a página de resultados do Google e escrever “HealthCheck IA — aguardando análise” embaixo dos resultados.**

A partir daí, construímos tudo em cima dela.


----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

Perfeito. Vamos considerar a **ETAPA 1 oficialmente iniciada** e transformar isso em uma especificação simples do produto, para a gente não se perder depois.

### MVP 1.0 — HealthCheck IA

**Objetivo:** criar uma extensão para Google Chrome que analise resultados de pesquisas relacionadas à saúde e apresente ao usuário uma avaliação baseada em evidências confiáveis.

**Fluxo principal:**

```text
Usuário pesquisa no Google
        ↓
Extensão identifica os resultados
        ↓
Captura título + descrição + URL
        ↓
Verifica se o assunto é relacionado à saúde
        ↓
Busca evidências confiáveis
        ↓
IA compara a alegação com as evidências
        ↓
Classifica o conteúdo
        ↓
Mostra resultado + explicação + fontes
```

A classificação inicial pode seguir este padrão:

```text
🟢 Sustentada pelas evidências

🟡 Parcialmente sustentada

🟠 Enganosa

🔴 Contradita pelas evidências

⚪ Não foi possível verificar
```

E podemos ter separadamente:

```text
Confiança da análise: 87%
```

Esse número significa **confiança do sistema na classificação**, e não “87% de chance de a notícia ser verdadeira”.

### O que entra no MVP

O sistema vai trabalhar apenas com **texto em português relacionado à saúde**, inicialmente a partir dos resultados de pesquisas do Google. Ele analisará título, descrição e URL e utilizará uma base de evidências selecionada por nós.

As fontes poderão incluir, por exemplo, Ministério da Saúde, Anvisa, Fiocruz, BVS, PubMed e outras instituições que definirmos durante a construção da base.

### O que não entra agora

Para evitar aumentar demais o projeto, a primeira versão não terá análise de imagens, vídeos, áudio, Instagram, WhatsApp, política, esportes, outros idiomas ou análise automática de qualquer conteúdo da internet.

Também não vamos tentar construir uma IA própria do zero.

### Um exemplo real

Usuário pesquisa:

```text
chá de boldo cura diabetes
```

O Google apresenta:

```text
Chá de boldo pode acabar com diabetes...
site-exemplo.com
```

Nossa extensão poderia inserir logo abaixo:

```text
🔴 Contradita pelas evidências

Confiança da análise: 91%

As evidências encontradas não sustentam
a alegação de que o chá de boldo seja capaz
de curar diabetes.

3 evidências encontradas.

[Ver evidências]
```

Ao clicar em **Ver evidências**, poderiam aparecer as fontes utilizadas.

### Critérios para considerarmos o MVP concluído

A versão 1.0 estará pronta quando conseguirmos fazer uma demonstração completa:

```text
Pesquisa Google
      ↓
Resultado aparece
      ↓
Extensão captura
      ↓
Backend recebe
      ↓
Base encontra evidências
      ↓
IA analisa
      ↓
Extensão apresenta classificação
      ↓
Usuário consegue consultar as evidências
```

Não precisa ser perfeito, não precisa funcionar para toda doença e nem para toda notícia da internet. Precisa demonstrar que **a arquitetura e a ideia funcionam**.

Então nosso controle fica:

```text
PROJETO — HEALTHCHECK IA

[X] 01 — Definir MVP
[ ] 02 — Criar extensão Chrome
[ ] 03 — Capturar resultados Google
[ ] 04 — Criar interface dos selos
[ ] 05 — Criar FastAPI
[ ] 06 — Conectar extensão + API
[ ] 07 — Criar base de saúde
[ ] 08 — Implementar embeddings
[ ] 09 — Implementar busca semântica
[ ] 10 — Implementar RAG
[ ] 11 — Integrar LLM
[ ] 12 — Criar classificação
[ ] 13 — Mostrar evidências
[ ] 14 — Criar testes
[ ] 15 — Avaliar o sistema
[ ] 16 — MLflow
[ ] 17 — Documentação/GitHub
[ ] 18 — Apresentação
```

**ETAPA 1 concluída. ✅**

Agora podemos entrar na **ETAPA 2 — criar a primeira extensão Chrome do zero**. Nessa etapa ainda não vamos usar IA: o primeiro objetivo será simplesmente fazer o Chrome mostrar **“HealthCheck IA — aguardando análise”** embaixo dos resultados do Google.

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------


Certo. Então agora entramos oficialmente na **ETAPA 2**.

O objetivo aqui é bem simples: criar uma extensão local do Chrome que rode na página do Google e escreva, abaixo dos resultados:

> 🔎 HealthCheck IA — aguardando análise

Nesta etapa, **não entra IA, FastAPI, Python, banco de dados nem RAG**. Vamos provar apenas que a extensão consegue entrar na página do Google e modificar visualmente os resultados.

Você vai precisar criar uma pasta assim:

```text
healthcheck-extension/
│
├── manifest.json
├── content.js
└── styles.css
```

O `manifest.json` define a extensão e quais páginas ela pode acessar:

```json
{
  "manifest_version": 3,
  "name": "HealthCheck IA",
  "version": "1.0",
  "description": "Extensão para auxiliar na verificação de informações de saúde.",
  "permissions": [],
  "content_scripts": [
    {
      "matches": [
        "https://www.google.com/*"
      ],
      "js": [
        "content.js"
      ],
      "css": [
        "styles.css"
      ]
    }
  ]
}
```

No `content.js`, vamos procurar os resultados do Google e inserir nosso aviso:

```javascript
function adicionarHealthCheck() {
  const resultados = document.querySelectorAll("div.MjjYud");

  resultados.forEach((resultado) => {
    if (resultado.querySelector(".healthcheck-box")) {
      return;
    }

    const titulo = resultado.querySelector("h3");

    if (!titulo) {
      return;
    }

    const caixa = document.createElement("div");

    caixa.className = "healthcheck-box";
    caixa.innerHTML = `
      <strong>🔎 HealthCheck IA</strong>
      <span>Aguardando análise</span>
    `;

    resultado.appendChild(caixa);
  });
}

adicionarHealthCheck();

const observer = new MutationObserver(() => {
  adicionarHealthCheck();
});

observer.observe(document.body, {
  childList: true,
  subtree: true
});
```

E no `styles.css`:

```css
.healthcheck-box {
    margin-top: 8px;
    padding: 10px 14px;

    border: 1px solid #bcd7f5;
    border-radius: 8px;

    background-color: #f4f9ff;

    font-family: Arial, sans-serif;
    font-size: 14px;

    display: flex;
    align-items: center;
    gap: 10px;

    color: #1f4e79;
}

.healthcheck-box strong {
    font-weight: bold;
}

.healthcheck-box span {
    color: #666;
}
```

Depois de criar esses três arquivos, abra no Chrome:

```text
chrome://extensions
```

Ative:

```text
Modo do desenvolvedor
```

Clique em:

```text
Carregar sem compactação
```

E selecione a pasta:

```text
healthcheck-extension
```

Depois abra o Google e pesquise, por exemplo:

```text
vitamina C cura gripe
```

Se tudo funcionar, abaixo dos resultados deverá aparecer:

```text
🔎 HealthCheck IA   Aguardando análise
```

Se aparecer, podemos marcar:

```text
[X] ETAPA 2 — Extensão Chrome básica
```

O que você precisa entender nesta etapa é principalmente: **`manifest.json` diz ao Chrome quando executar a extensão; `content.js` entra na página e manipula o DOM; `styles.css` controla o visual; `MutationObserver` observa mudanças porque o Google pode adicionar resultados dinamicamente.**

Quando você criar esses três arquivos e testar, pode me mandar **print da tela ou qualquer erro que aparecer**. Aí eu vou corrigindo com você até a extensão funcionar.


--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

Perfeito. Agora vamos fazer a **ETAPA 3** de forma bem controlada.

O objetivo é fazer o `content.js` capturar, para cada resultado do Google:

```text
Título
URL
Descrição
```

e mostrar tudo no console.

Substitua o conteúdo atual do seu `content.js` por este:

```javascript
function capturarResultados() {
  const resultados = document.querySelectorAll("div.MjjYud");

  const dados = [];

  resultados.forEach((resultado, index) => {
    const tituloElemento = resultado.querySelector("h3");
    const linkElemento = tituloElemento?.closest("a");

    // Alguns resultados podem não ter descrição
    const descricaoElemento =
      resultado.querySelector(".VwiC3b") ||
      resultado.querySelector("[data-sncf]");

    if (!tituloElemento || !linkElemento) {
      return;
    }

    const item = {
      titulo: tituloElemento.innerText.trim(),
      url: linkElemento.href,
      descricao: descricaoElemento
        ? descricaoElemento.innerText.trim()
        : "Descrição não encontrada"
    };

    dados.push(item);

    console.log(`Resultado ${dados.length}`);
    console.log("Título:", item.titulo);
    console.log("URL:", item.url);
    console.log("Descrição:", item.descricao);
    console.log("-----------------------------");
  });

  console.log("Todos os resultados capturados:");
  console.log(dados);

  return dados;
}

capturarResultados();
```

Agora salve o arquivo.

Depois vá em:

```text
chrome://extensions
```

Encontre o **HealthCheck IA** e clique no botão de:

```text
↻ Recarregar
```

Depois volte ao Google e faça uma pesquisa, por exemplo:

```text
vitamina C cura gripe
```

Agora abra o console:

```text
F12
```

e vá na aba:

```text
Console
```

Você deve ver algo parecido com:

```text
Resultado 1

Título: Vitamina C ajuda no tratamento da gripe?

URL: https://exemplo.com/artigo

Descrição: Saiba o que estudos científicos dizem...

-----------------------------

Resultado 2

Título: Vitamina C previne resfriados?

URL: https://outrosite.com/artigo

Descrição: Pesquisas analisam os efeitos...

-----------------------------
```

E no final:

```javascript
[
  {
    titulo: "...",
    url: "...",
    descricao: "..."
  },
  {
    titulo: "...",
    url: "...",
    descricao: "..."
  }
]
```

O ponto mais importante dessa etapa é você entender o que cada linha faz.

`document.querySelectorAll("div.MjjYud")` procura **vários elementos** da página do Google que representam resultados.

`resultado.querySelector("h3")` procura o título dentro daquele resultado.

`tituloElemento?.closest("a")` sobe na estrutura HTML até encontrar o link `<a>` associado ao título.

Então conseguimos:

```javascript
tituloElemento.innerText
```

para pegar o texto do título.

E:

```javascript
linkElemento.href
```

para pegar a URL.

Já a descrição é procurada aqui:

```javascript
resultado.querySelector(".VwiC3b")
```

Esse nome vem da estrutura atual do Google e pode mudar no futuro. Isso é normal em scraping/manipulação de DOM de páginas que não controlamos.

Depois criamos um **objeto JavaScript**:

```javascript
const item = {
  titulo: "...",
  url: "...",
  descricao: "..."
};
```

E colocamos esse objeto dentro de um array:

```javascript
dados.push(item);
```

Então conceitualmente temos:

```text
Google
   ↓
querySelectorAll()
   ↓
resultado individual
   ↓
querySelector()
   ↓
título
URL
descrição
   ↓
objeto JavaScript
   ↓
array
```

### Um detalhe importante

Nesta etapa, **não coloque ainda IA nem classificação**.

A única pergunta que queremos responder agora é:

> "Minha extensão consegue transformar os resultados visuais do Google em dados estruturados?"

Se a resposta for sim, a Etapa 3 está praticamente concluída.

Quando testar, me mande o que apareceu no Console. Se algum campo vier vazio, URL errada ou descrição como `"Descrição não encontrada"`, eu ajusto o seletor com você.


--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------


Sim! 🎉 **A ETAPA 3 funcionou.** O resultado do seu console mostra exatamente o que queríamos obter.

Você conseguiu capturar **10 resultados do Google** e transformar cada um em um objeto JavaScript com:

```javascript
{
    titulo: "...",
    url: "...",
    descricao: "..."
}
```

Por exemplo, seu Resultado 1 foi capturado corretamente:

```text
Resultado 1

Título:
Vitamina C previne gripe e resfriados?

URL:
https://drauziovarella.uol.com.br/...

Descrição:
A vitamina C não reduz de forma significativa...
```

E, mais importante ainda, no final você tem:

```text
Todos os resultados capturados:

Array(10)
```

Ou seja, conceitualmente sua extensão já fez:

```text
Página do Google
      ↓
Encontra os resultados no DOM
      ↓
querySelectorAll()
      ↓
Percorre cada resultado
      ↓
Extrai:
Título
URL
Descrição
      ↓
Cria objetos JavaScript
      ↓
dados.push(item)
      ↓
Array com 10 objetos
```

### Um pequeno detalhe que NÃO é erro

No Resultado 2 apareceu:

```text
Descrição: Descrição não encontrada
```

Isso é esperado. O Google não necessariamente usa exatamente a mesma estrutura HTML para todos os tipos de resultados.

E nosso código já foi preparado para lidar com isso:

```javascript
descricao: descricaoElemento
    ? descricaoElemento.innerText.trim()
    : "Descrição não encontrada"
```

Portanto, ele não quebrou. Pelo contrário: **tratou corretamente a ausência do elemento.**

Esse tipo de tratamento vai ser importante mais adiante porque não podemos presumir que todo resultado do Google terá os mesmos campos.

## 🏆 Status do projeto

```text
HEALTHCHECK IA

[X] ETAPA 1 — Definir o MVP
[X] ETAPA 2 — Extensão Chrome básica
[X] ETAPA 3 — Capturar resultados do Google ✅

[ ] ETAPA 4 — Criar interface dos selos
[ ] ETAPA 5 — Criar FastAPI
[ ] ETAPA 6 — Conectar extensão + API
[ ] ETAPA 7 — Criar base de saúde
[ ] ETAPA 8 — Embeddings
[ ] ETAPA 9 — Busca semântica
[ ] ETAPA 10 — RAG
[ ] ETAPA 11 — LLM
[ ] ETAPA 12 — Classificação
[ ] ETAPA 13 — Evidências/fontes
[ ] ETAPA 14 — Pensamento crítico / UX
[ ] ETAPA 15 — Testes
[ ] ETAPA 16 — Métricas
[ ] ETAPA 17 — MLflow
[ ] ETAPA 18 — Limitações
[ ] ETAPA 19 — GitHub
[ ] ETAPA 20 — Demonstração
[ ] ETAPA 21 — Apresentação
```

E agora temos uma coisa muito importante: **dados estruturados**.

Antes o Google tinha apenas elementos HTML. Agora o HealthCheck consegue transformar esses elementos em informação que nosso sistema consegue processar:

```javascript
const dados = [
    {
        titulo: "Vitamina C previne gripe e resfriados?",
        url: "https://...",
        descricao: "A vitamina C não reduz..."
    },

    {
        titulo: "Vitamina C Para Diabetes",
        url: "https://...",
        descricao: "Descrição não encontrada"
    },

    // ...
];
```

Mais para frente, é justamente algo parecido com isso que vamos mandar para:

**Extensão → FastAPI → RAG → evidências → LLM → classificação → extensão.**

**ETAPA 3 CONCLUÍDA. ✅**

Agora podemos partir para a **ETAPA 4 — transformar aquele simples “🔎 HealthCheck IA — aguardando análise” em um selo/card visual profissional abaixo de cada resultado.**


-----------------------------------------------------------------------------------------------------------------------------------------------------


Perfeito. Na **ETAPA 4** vamos manter tudo simulado, mas deixar o HealthCheck IA com cara de produto de verdade.

A ideia é: em vez do texto simples “aguardando análise”, cada resultado do Google vai receber um **card visual**, com status, confiança e um botão expansível.

Substitua seu `content.js` por este:

```javascript
function capturarResultados() {
  const resultados = document.querySelectorAll("div.MjjYud");

  resultados.forEach((resultado, index) => {
    const tituloElemento = resultado.querySelector("h3");
    const linkElemento = tituloElemento?.closest("a");

    const descricaoElemento =
      resultado.querySelector(".VwiC3b") ||
      resultado.querySelector("[data-sncf]");

    if (!tituloElemento || !linkElemento) {
      return;
    }

    // Evita inserir o componente duas vezes
    if (resultado.querySelector(".healthcheck-card")) {
      return;
    }

    const item = {
      titulo: tituloElemento.innerText.trim(),
      url: linkElemento.href,
      descricao: descricaoElemento
        ? descricaoElemento.innerText.trim()
        : "Descrição não encontrada"
    };

    criarComponenteHealthCheck(resultado, item, index);
  });
}


function criarComponenteHealthCheck(resultado, item, index) {
  const card = document.createElement("div");

  card.className = "healthcheck-card";

  /*
    Por enquanto os resultados são SIMULADOS.

    Alternamos entre verde e vermelho apenas para
    visualizar os dois tipos de componente.
  */

  const favoravel = index % 2 === 0;

  const status = favoravel
    ? "Evidências favoráveis"
    : "Evidências contraditórias";

  const classeStatus = favoravel
    ? "healthcheck-favoravel"
    : "healthcheck-contraditorio";

  const icone = favoravel ? "🟢" : "🔴";

  const confianca = favoravel ? 85 : 82;

  card.innerHTML = `
    <div class="healthcheck-header">

      <div class="healthcheck-status ${classeStatus}">
        <span>${icone}</span>
        <strong>${status}</strong>
      </div>

      <div class="healthcheck-confidence">
        Confiança: ${confianca}%
      </div>

    </div>

    <button
      class="healthcheck-button"
      aria-expanded="false"
      type="button"
    >
      Ver análise
    </button>

    <div
      class="healthcheck-details"
      hidden
    >

      <p>
        Esta análise é apenas uma simulação visual.
        A Inteligência Artificial ainda não está conectada.
      </p>

      <p>
        <strong>Título analisado:</strong>
        ${item.titulo}
      </p>

      <p>
        <strong>Fonte:</strong>
        ${new URL(item.url).hostname}
      </p>

    </div>
  `;

  resultado.appendChild(card);

  const botao = card.querySelector(".healthcheck-button");
  const detalhes = card.querySelector(".healthcheck-details");

  botao.addEventListener("click", () => {
    const estaAberto =
      botao.getAttribute("aria-expanded") === "true";

    botao.setAttribute(
      "aria-expanded",
      String(!estaAberto)
    );

    detalhes.hidden = estaAberto;

    botao.innerText = estaAberto
      ? "Ver análise"
      : "Ocultar análise";
  });
}


function iniciarHealthCheck() {
  capturarResultados();
}


iniciarHealthCheck();


const observer = new MutationObserver(() => {
  capturarResultados();
});


observer.observe(document.body, {
  childList: true,
  subtree: true
});
```

Agora substitua seu `styles.css` por este:

```css
.healthcheck-card {
  margin-top: 12px;
  margin-bottom: 12px;

  padding: 14px 16px;

  max-width: 600px;

  background: #ffffff;

  border: 1px solid #dfe6ee;
  border-radius: 12px;

  box-shadow:
    0 2px 6px rgba(0, 0, 0, 0.06);

  font-family:
    Arial,
    Helvetica,
    sans-serif;

  color: #202124;
}


/* =========================
   CABEÇALHO
========================= */

.healthcheck-header {
  display: flex;

  justify-content: space-between;
  align-items: center;

  gap: 12px;

  margin-bottom: 12px;
}


/* =========================
   STATUS
========================= */

.healthcheck-status {
  display: flex;

  align-items: center;

  gap: 7px;

  font-size: 14px;
}


.healthcheck-favoravel {
  color: #137333;
}


.healthcheck-contraditorio {
  color: #b3261e;
}


/* =========================
   CONFIANÇA
========================= */

.healthcheck-confidence {
  font-size: 12px;

  color: #5f6368;

  background: #f1f3f4;

  padding: 5px 9px;

  border-radius: 20px;
}


/* =========================
   BOTÃO
========================= */

.healthcheck-button {
  background: transparent;

  border: none;

  padding: 0;

  color: #1a73e8;

  font-size: 13px;
  font-weight: 600;

  cursor: pointer;
}


.healthcheck-button:hover {
  text-decoration: underline;
}


.healthcheck-button:focus {
  outline: 2px solid #1a73e8;

  outline-offset: 3px;

  border-radius: 3px;
}


/* =========================
   DETALHES
========================= */

.healthcheck-details {
  margin-top: 14px;

  padding-top: 12px;

  border-top: 1px solid #e8eaed;

  font-size: 13px;

  line-height: 1.5;

  color: #3c4043;
}


.healthcheck-details p {
  margin: 7px 0;
}
```

Seu `manifest.json` pode continuar praticamente igual ao da etapa anterior:

```json
{
  "manifest_version": 3,
  "name": "HealthCheck IA",
  "version": "1.0",
  "description": "Extensão para auxiliar na verificação de informações de saúde.",
  "content_scripts": [
    {
      "matches": [
        "https://www.google.com/*"
      ],
      "js": [
        "content.js"
      ],
      "css": [
        "styles.css"
      ]
    }
  ]
}
```

Depois faça novamente:

```text
1. Salvar content.js
2. Salvar styles.css
3. Abrir chrome://extensions
4. Recarregar HealthCheck IA
5. Atualizar a página do Google
6. Fazer uma pesquisa
```

Você deverá começar a ver algo como:

```text
🟢 Evidências favoráveis             Confiança: 85%

Ver análise
```

No resultado seguinte:

```text
🔴 Evidências contraditórias         Confiança: 82%

Ver análise
```

E ao clicar em **Ver análise**, o card expande:

```text
Esta análise é apenas uma simulação visual.
A Inteligência Artificial ainda não está conectada.

Título analisado:
Vitamina C previne gripe e resfriados?

Fonte:
drauziovarella.uol.com.br

Ocultar análise
```

Um detalhe muito importante: os valores de **85% e 82% são totalmente falsos nesta etapa**. Eles estão ali apenas para construir e testar a interface. Não estamos calculando nada ainda.

O `index % 2 === 0` também não faz qualquer análise:

```javascript
const favoravel = index % 2 === 0;
```

Ele simplesmente faz:

```text
Resultado 1 → verde
Resultado 2 → vermelho
Resultado 3 → verde
Resultado 4 → vermelho
...
```

É só para conseguirmos enxergar os dois estados da interface.

Nesta etapa você está aprendendo cinco conceitos importantes ao mesmo tempo: **componentes**, porque cada resultado recebe um card independente; **eventos**, porque o botão reage ao clique; **estado**, porque o componente sabe se está aberto ou fechado; **CSS**, porque estamos estilizando elementos inseridos pela extensão; e **acessibilidade**, usando `button`, `aria-expanded`, foco visual e `hidden`.

Quando estiver funcionando, nossa arquitetura já terá evoluído para:

```text
Google
   ↓
HealthCheck encontra o resultado
   ↓
captura título + URL + descrição
   ↓
cria componente visual
   ↓
┌──────────────────────────────┐
│ 🟢 Evidências favoráveis     │
│ Confiança: 85%               │
│                              │
│ Ver análise                  │
└──────────────────────────────┘
```

Aí podemos marcar **ETAPA 4 concluída** e partir para a primeira parte fora do navegador: **ETAPA 5 — criar nossa API em Python com FastAPI.**


--------------------------------------------------------------------------------------------------------------------------------------------------------------

Sim, **ficaria exatamente assim**. Esse `content.js` está coerente com a correção que fizemos.

Só tem um detalhe: do jeito que está, o `contadorResultados` reinicia em `0` toda vez que o `MutationObserver` chama `capturarResultados()`. Como os cards já inseridos são ignorados, isso não costuma quebrar, mas pode fazer novos resultados carregados depois começarem novamente pelo verde.

Para a **Etapa 4**, isso é totalmente aceitável. O importante é você ver agora a alternância:

```text
🟢 Evidências favoráveis — 85%
🔴 Evidências contraditórias — 82%
🟢 Evidências favoráveis — 85%
🔴 Evidências contraditórias — 82%
```

Então faça:

```text
1. Salve o content.js
2. Vá em chrome://extensions
3. Recarregue o HealthCheck IA
4. Volte ao Google
5. Atualize a página com Ctrl + F5
6. Faça a pesquisa novamente
```

Se aparecer verde e vermelho alternando, a **ETAPA 4 está concluída**. ✅

E reforçando: esses resultados ainda são **simulados**. O código:

```javascript
const favoravel = index % 2 === 0;
```

não está analisando se a notícia é verdadeira ou falsa. Ele só usa a posição do resultado para escolher a aparência do card. A análise real só vai surgir quando conectarmos o backend e, mais adiante, o RAG/LLM.


----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

Perfeito. Agora entramos na **ETAPA 5 — Backend Python com FastAPI**.

A ideia é criar um servidor separado da extensão. A extensão será a interface no navegador; o backend será onde ficará a lógica do sistema.

Crie uma pasta nova ao lado da extensão:

```text
healthcheck-ia/
│
├── extension/
│   ├── manifest.json
│   ├── content.js
│   └── styles.css
│
└── backend/
    └── main.py
```

Dentro da pasta `backend`, abra o terminal e instale:

```bash
pip install fastapi uvicorn
```

Agora crie o arquivo `main.py`:

```python
from fastapi import FastAPI
from pydantic import BaseModel
from fastapi.middleware.cors import CORSMiddleware

app = FastAPI(
    title="HealthCheck IA API",
    version="1.0.0"
)


# =========================
# CORS
# =========================

app.add_middleware(
    CORSMiddleware,
    allow_origins=["*"],
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)


# =========================
# MODELOS DE DADOS
# =========================

class AnaliseRequest(BaseModel):
    titulo: str
    descricao: str


class AnaliseResponse(BaseModel):
    classificacao: str
    confianca: float


# =========================
# ROTAS
# =========================

@app.get("/")
def raiz():
    return {
        "mensagem": "HealthCheck IA API funcionando"
    }


@app.post(
    "/analisar",
    response_model=AnaliseResponse
)
def analisar(dados: AnaliseRequest):

    print("Título recebido:", dados.titulo)
    print("Descrição recebida:", dados.descricao)

    # RESPOSTA SIMULADA
    return AnaliseResponse(
        classificacao="enganosa",
        confianca=0.82
    )
```

Depois, dentro da pasta `backend`, rode:

```bash
uvicorn main:app --reload
```

Se funcionar, você deverá ver algo parecido com:

```text
Uvicorn running on http://127.0.0.1:8000
```

Abra no navegador:

```text
http://127.0.0.1:8000
```

Deve aparecer:

```json
{
  "mensagem": "HealthCheck IA API funcionando"
}
```

Agora vem uma parte muito útil do FastAPI. Abra:

```text
http://127.0.0.1:8000/docs
```

Você verá automaticamente uma documentação interativa da API.

Procure:

```text
POST /analisar
```

Clique em:

```text
Try it out
```

E envie:

```json
{
  "titulo": "Vitamina C cura gripe?",
  "descricao": "A vitamina C pode curar gripe rapidamente."
}
```

A resposta esperada será:

```json
{
  "classificacao": "enganosa",
  "confianca": 0.82
}
```

Se isso acontecer, teremos conseguido:

```text
CLIENTE
   ↓
HTTP POST
   ↓
JSON
   ↓
FASTAPI
   ↓
PYDANTIC
   ↓
função analisar()
   ↓
JSON
   ↓
RESPOSTA
```

Agora, os conceitos importantes desta etapa.

**API** é a interface pela qual dois sistemas se comunicam. No nosso caso, futuramente:

```text
Chrome Extension
       ↓
      API
       ↓
Backend Python
```

**HTTP** é o protocolo usado nessa comunicação.

**GET** normalmente busca informações:

```http
GET /
```

**POST** envia informações para processamento:

```http
POST /analisar
```

Por isso usamos POST para análise. Estamos enviando conteúdo para o backend processar.

O corpo da requisição será JSON:

```json
{
  "titulo": "...",
  "descricao": "..."
}
```

E o backend responde também em JSON:

```json
{
  "classificacao": "enganosa",
  "confianca": 0.82
}
```

O **Pydantic** entra aqui:

```python
class AnaliseRequest(BaseModel):
    titulo: str
    descricao: str
```

Ele define o contrato de entrada.

Se alguém mandar:

```json
{
  "titulo": 123,
  "descricao": []
}
```

o FastAPI/Pydantic pode rejeitar os dados porque não correspondem ao formato esperado.

Isso é importante porque nosso sistema não deve simplesmente aceitar qualquer estrutura.

Já:

```python
class AnaliseResponse(BaseModel):
    classificacao: str
    confianca: float
```

define o formato esperado da resposta.

E o **CORS**:

```python
app.add_middleware(
    CORSMiddleware,
    allow_origins=["*"],
    allow_methods=["*"],
    allow_headers=["*"],
)
```

será importante quando a extensão Chrome começar a fazer requisições para:

```text
http://127.0.0.1:8000
```

Nesta etapa estamos liberando tudo porque é um ambiente local de desenvolvimento. Mais tarde podemos restringir isso.

E uma observação essencial:

```python
return AnaliseResponse(
    classificacao="enganosa",
    confianca=0.82
)
```

**não existe nenhuma inteligência aí ainda.**

Todo texto enviado receberá:

```text
enganosa
82%
```

Mesmo se você mandar:

```json
{
  "titulo": "A Terra é redonda",
  "descricao": "..."
}
```

ele continuará respondendo:

```json
{
  "classificacao": "enganosa",
  "confianca": 0.82
}
```

Porque estamos apenas construindo a infraestrutura.

Nossa arquitetura agora começa a ficar assim:

```text
┌─────────────────────┐
│       GOOGLE        │
└──────────┬──────────┘
           ↓
┌─────────────────────┐
│ CHROME EXTENSION    │
│ JavaScript          │
└─────────────────────┘


┌─────────────────────┐
│ BACKEND             │
│ Python + FastAPI    │
│                     │
│ POST /analisar      │
└─────────────────────┘
```

Ainda estão separados.

A **ETAPA 6** vai conectar os dois:

```text
Google
  ↓
Extensão
  ↓
fetch()
  ↓
POST /analisar
  ↓
FastAPI
  ↓
JSON
  ↓
Extensão
  ↓
Card do HealthCheck
```

Para concluir a **ETAPA 5**, precisamos confirmar três coisas:

```text
[ ] http://127.0.0.1:8000 abre

[ ] http://127.0.0.1:8000/docs abre

[ ] POST /analisar retorna:
    classificacao = enganosa
    confianca = 0.82
```

Se essas três coisas funcionarem, marcamos **ETAPA 5 concluída**.

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

Perfeito. Agora vamos para a **ETAPA 6 — conectar a extensão Chrome com o backend Python**.

Aqui acontece a primeira comunicação real entre o `content.js` e o FastAPI.

Seu backend precisa estar rodando com:

```bash
uvicorn main:app --reload
```

e acessível em:

```text
http://127.0.0.1:8000
```

Agora vamos alterar o `content.js`.

A ideia será esta:

```text
resultado do Google
↓
captura título + descrição
↓
fetch()
↓
POST /analisar
↓
FastAPI
↓
JSON
↓
JavaScript recebe
↓
card muda sozinho
```

Use este `content.js`:

```javascript
async function analisarResultado(item) {
  try {
    const resposta = await fetch("http://127.0.0.1:8000/analisar", {
      method: "POST",

      headers: {
        "Content-Type": "application/json"
      },

      body: JSON.stringify({
        titulo: item.titulo,
        descricao: item.descricao
      })
    });

    if (!resposta.ok) {
      throw new Error(`Erro HTTP: ${resposta.status}`);
    }

    const dados = await resposta.json();

    console.log("Resposta do backend:", dados);

    return dados;

  } catch (erro) {
    console.error("Erro ao conectar com o backend:", erro);

    return null;
  }
}


async function capturarResultados() {
  const resultados = document.querySelectorAll("div.MjjYud");

  for (const resultado of resultados) {

    const tituloElemento = resultado.querySelector("h3");
    const linkElemento = tituloElemento?.closest("a");

    const descricaoElemento =
      resultado.querySelector(".VwiC3b") ||
      resultado.querySelector("[data-sncf]");

    if (!tituloElemento || !linkElemento) {
      continue;
    }

    if (resultado.querySelector(".healthcheck-card")) {
      continue;
    }

    const item = {
      titulo: tituloElemento.innerText.trim(),

      url: linkElemento.href,

      descricao: descricaoElemento
        ? descricaoElemento.innerText.trim()
        : "Descrição não encontrada"
    };

    criarComponenteHealthCheck(resultado, item);
  }
}


async function criarComponenteHealthCheck(resultado, item) {

  const card = document.createElement("div");

  card.className = "healthcheck-card";

  /*
    Primeiro mostramos o estado de carregamento.
  */

  card.innerHTML = `
    <div class="healthcheck-header">

      <div class="healthcheck-status">
        <span>🔎</span>

        <strong>
          HealthCheck IA
        </strong>
      </div>

      <div class="healthcheck-confidence">
        Analisando...
      </div>

    </div>

    <div class="healthcheck-loading">
      Consultando o backend...
    </div>
  `;

  resultado.appendChild(card);


  /*
    Agora enviamos o conteúdo para o FastAPI.
  */

  const analise = await analisarResultado(item);


  /*
    Caso não seja possível conectar ao backend.
  */

  if (!analise) {

    card.innerHTML = `
      <div class="healthcheck-header">

        <div class="healthcheck-status healthcheck-erro">
          <span>⚠️</span>

          <strong>
            Não foi possível analisar
          </strong>
        </div>

      </div>

      <p>
        O servidor HealthCheck IA não respondeu.
      </p>
    `;

    return;
  }


  /*
    Por enquanto nosso backend sempre retorna:

    {
      classificacao: "enganosa",
      confianca: 0.82
    }

    Portanto ainda é uma resposta simulada.
  */


  const classificacao = analise.classificacao;

  const confianca = Math.round(
    analise.confianca * 100
  );


  let status;
  let icone;
  let classeStatus;


  if (classificacao === "enganosa") {

    status = "Evidências contraditórias";

    icone = "🔴";

    classeStatus = "healthcheck-contraditorio";

  } else {

    status = "Resultado recebido";

    icone = "🟡";

    classeStatus = "";
  }


  card.innerHTML = `
    <div class="healthcheck-header">

      <div class="healthcheck-status ${classeStatus}">

        <span>
          ${icone}
        </span>

        <strong>
          ${status}
        </strong>

      </div>

      <div class="healthcheck-confidence">

        Confiança:
        ${confianca}%

      </div>

    </div>


    <button
      class="healthcheck-button"
      aria-expanded="false"
      type="button"
    >

      Ver análise

    </button>


    <div
      class="healthcheck-details"
      hidden
    >

      <p>

        <strong>
          Classificação recebida do backend:
        </strong>

        ${classificacao}

      </p>


      <p>

        <strong>
          Título analisado:
        </strong>

        ${item.titulo}

      </p>


      <p>

        <strong>
          Fonte:
        </strong>

        ${new URL(item.url).hostname}

      </p>


      <p class="healthcheck-warning">

        Esta classificação ainda é simulada.
        A Inteligência Artificial ainda não foi implementada.

      </p>

    </div>
  `;


  const botao =
    card.querySelector(".healthcheck-button");

  const detalhes =
    card.querySelector(".healthcheck-details");


  botao.addEventListener("click", () => {

    const estaAberto =
      botao.getAttribute("aria-expanded") === "true";


    botao.setAttribute(
      "aria-expanded",
      String(!estaAberto)
    );


    detalhes.hidden = estaAberto;


    botao.innerText = estaAberto
      ? "Ver análise"
      : "Ocultar análise";

  });
}



capturarResultados();


const observer = new MutationObserver(() => {

  capturarResultados();

});


observer.observe(
  document.body,
  {
    childList: true,
    subtree: true
  }
);
```

Seu `styles.css` pode continuar praticamente igual. Só acrescente no final:

```css
.healthcheck-loading {
  font-size: 13px;
  color: #5f6368;
}

.healthcheck-erro {
  color: #b3261e;
}

.healthcheck-warning {
  margin-top: 12px;
  padding: 10px;

  background: #fff8e1;

  border-left: 3px solid #f9ab00;

  border-radius: 4px;
}
```

Agora precisamos fazer uma pequena alteração no `manifest.json`, porque a extensão vai acessar nosso servidor local.

Deixe assim:

```json
{
  "manifest_version": 3,

  "name": "HealthCheck IA",

  "version": "1.0",

  "description": "Extensão para auxiliar na verificação de informações de saúde.",

  "host_permissions": [
    "http://127.0.0.1:8000/*"
  ],

  "content_scripts": [
    {
      "matches": [
        "https://www.google.com/*"
      ],

      "js": [
        "content.js"
      ],

      "css": [
        "styles.css"
      ]
    }
  ]
}
```

Agora faça nessa ordem:

1. Deixe o FastAPI ligado:

```bash
uvicorn main:app --reload
```

2. Confirme que:

```text
http://127.0.0.1:8000
```

está funcionando.

3. Salve `content.js`.

4. Salve `styles.css`.

5. Salve `manifest.json`.

6. Vá em:

```text
chrome://extensions
```

7. Clique em **Recarregar** no HealthCheck IA.

8. Volte ao Google e dê:

```text
Ctrl + F5
```

9. Pesquise novamente:

```text
vitamina C cura gripe
```

Agora observe o card.

Ele deve aparecer primeiro assim:

```text
🔎 HealthCheck IA                Analisando...

Consultando o backend...
```

e depois mudar automaticamente para:

```text
🔴 Evidências contraditórias     Confiança: 82%

Ver análise
```

Isso será muito diferente da Etapa 4.

Na Etapa 4, o JavaScript inventava o resultado:

```javascript
const confianca = 82;
```

Agora não.

Quem envia isso é o Python:

```python
return AnaliseResponse(
    classificacao="enganosa",
    confianca=0.82
)
```

O navegador recebe aqui:

```javascript
const dados = await resposta.json();
```

Portanto teremos uma comunicação real:

```text
Chrome

content.js
   ↓
fetch()
   ↓

HTTP POST

{
 "titulo": "...",
 "descricao": "..."
}

   ↓

FastAPI

POST /analisar

   ↓

Python

   ↓

{
 "classificacao": "enganosa",
 "confianca": 0.82
}

   ↓

HTTP Response

   ↓

JavaScript

   ↓

Math.round(0.82 * 100)

   ↓

82%

   ↓

CARD DO GOOGLE
```

Um detalhe importante: como o backend atualmente retorna `"enganosa"` para qualquer entrada, **todos os cards provavelmente ficarão vermelhos com 82% novamente**.

Mas agora isso estará correto.

Antes:

```text
vermelho → inventado pelo JavaScript
```

Agora:

```text
vermelho
   ↑
JSON
   ↑
FastAPI
   ↑
Python
```

É exatamente isso que queremos comprovar na Etapa 6.

Para verificar ainda melhor, abra o `F12 → Console`.

Você deve encontrar mensagens como:

```text
Resposta do backend:
{
    classificacao: "enganosa",
    confianca: 0.82
}
```

E no terminal onde está o FastAPI provavelmente aparecerão chamadas como:

```text
POST /analisar HTTP/1.1 200 OK
```

Se o card mudar de **“Analisando...”** para **“Evidências contraditórias — Confiança: 82%”**, e o backend registrar os `POST /analisar`, então podemos declarar:

**[X] ETAPA 6 — Chrome ↔ Python concluída. ✅**


------------------------------------------------------------------------------------------------------------------------------------

UHUUU! 🚀 **ETAPA 6 oficialmente concluída!** E essa foi uma etapa importante porque agora vocês têm uma arquitetura **cliente-servidor funcionando de verdade**.

```text
HEALTHCHECK IA — PROGRESSO

[X] ETAPA 1 — Definir o MVP
[X] ETAPA 2 — Extensão Chrome básica
[X] ETAPA 3 — Capturar resultados do Google
[X] ETAPA 4 — Componente visual
[X] ETAPA 5 — Backend Python + FastAPI
[X] ETAPA 6 — Conectar Chrome ↔ Python ✅

[ ] ETAPA 7 — Base de evidências de saúde
[ ] ETAPA 8 — Embeddings
[ ] ETAPA 9 — Busca semântica
[ ] ETAPA 10 — RAG
[ ] ETAPA 11 — LLM local
[ ] ETAPA 12 — Classificação
[ ] ETAPA 13 — Evidências e fontes
[ ] ETAPA 14 — Pensamento crítico / UX
[ ] ETAPA 15 — Testes
[ ] ETAPA 16 — Métricas
[ ] ETAPA 17 — MLflow
...
```

O sistema já executa este fluxo real:

```text
GOOGLE
   ↓
Extensão Chrome
   ↓
Captura título + descrição + URL
   ↓
JavaScript
   ↓
fetch()
   ↓
HTTP POST
   ↓
FastAPI / Python
   ↓
POST /analisar
   ↓
JSON
   ↓
Extensão recebe a resposta
   ↓
Atualiza o card no Google
```

Até agora, a única parte "falsa" é **a inteligência**. O backend ainda responde manualmente:

```json
{
  "classificacao": "enganosa",
  "confianca": 0.82
}
```

Mas a infraestrutura necessária para substituir isso por uma análise real já está funcionando.

A **ETAPA 7 — Base de evidências de saúde** começa justamente essa mudança. Em vez de o Python simplesmente inventar `"enganosa"`, vamos começar a construir o conhecimento confiável que posteriormente será recuperado pelo RAG.

Aí entram fontes como **Ministério da Saúde, Anvisa, Fiocruz, BVS, PubMed, universidades e sociedades médicas**, com uma preocupação importante: guardar não apenas o texto, mas também **fonte, título, URL, data e outros metadados**, para que futuramente o HealthCheck consiga mostrar ao usuário **qual evidência sustentou a análise**.

**6 etapas concluídas. Agora começa a construção da parte realmente inteligente do HealthCheck IA. 🔥**


------------------------------------------------------------------------------------------------------------------------------------------------------------

Perfeito. A **ETAPA 7** deve começar pequena e controlada. Antes de automatizar coleta de sites ou PubMed, o melhor é montar uma **base local confiável e rastreável**, com poucos documentos, mas bem organizados.

A estrutura pode ficar assim:

```text
healthcheck-extension/
│
├── backend/
│   └── main.py
│
├── extension/
│   ├── manifest.json
│   ├── content.js
│   └── styles.css
│
└── data/
    ├── vacinas/
    ├── diabetes/
    ├── hipertensao/
    ├── cancer/
    ├── dengue/
    └── medicamentos/
```

Dentro de cada pasta, inicialmente eu sugiro usar arquivos `.txt` ou `.md`, porque são simples de ler com Python e depois transformar em embeddings.

Por exemplo:

```text
data/
└── vacinas/
    ├── ministerio_saude_vacina_gripe.md
    ├── fiocruz_vacinas.md
    └── anvisa_vacinas.md
```

Um documento pode ter este formato:

```markdown
# Vacina contra influenza

Fonte: Ministério da Saúde
URL: https://...
Data de acesso: 09/09/2026
Tema: Vacinas

## Conteúdo

A vacina contra influenza é utilizada para reduzir o risco de formas graves da doença e suas complicações.

A vacina não causa gripe, pois as vacinas utilizadas na campanha não contêm vírus influenza capazes de provocar a doença.

## Palavras-chave

influenza, gripe, vacina, vacinação, prevenção
```

Esse formato é importante porque não queremos guardar apenas o texto. Precisamos preservar também os **metadados**:

```text
conteúdo
fonte
URL
tema
data
título
```

Mais tarde, quando o HealthCheck encontrar uma evidência, ele poderá devolver algo como:

```json
{
  "evidencia": "A vacina contra influenza não causa gripe...",
  "fonte": "Ministério da Saúde",
  "url": "https://...",
  "tema": "vacinas"
}
```

E isso vai permitir que o usuário clique em **Ver evidências** e veja de onde a informação veio.

### O primeiro conjunto da base

Eu não tentaria agora colocar centenas de documentos. Para o MVP, começaria com algo como:

```text
VACINAS
3 a 5 documentos

DIABETES
3 a 5 documentos

HIPERTENSÃO
3 a 5 documentos

DENGUE
3 a 5 documentos

MEDICAMENTOS
3 a 5 documentos
```

Isso já nos daria algo perto de **15 a 25 documentos confiáveis** para testar o sistema.

Cancer pode entrar depois, porque é um assunto extremamente amplo.

Outra coisa importante: **não precisamos armazenar artigos científicos inteiros**. Para o MVP, podemos guardar trechos relevantes, resumos ou documentos públicos selecionados, sempre mantendo a referência da fonte.

### Antes dos embeddings

Nesta etapa ainda não vamos fazer busca semântica. Primeiro precisamos provar que o Python consegue ler nossa base.

Depois de criar alguns arquivos, podemos fazer algo simples como:

```python
from pathlib import Path

pasta_data = Path("../data")

documentos = []

for arquivo in pasta_data.rglob("*.md"):
    conteudo = arquivo.read_text(
        encoding="utf-8"
    )

    documentos.append({
        "arquivo": arquivo.name,
        "conteudo": conteudo
    })

print(f"Documentos carregados: {len(documentos)}")

for documento in documentos:
    print(documento["arquivo"])
```

Se tivermos:

```text
data/
├── vacinas/
│   ├── vacina_gripe.md
│   └── vacina_covid.md
│
└── diabetes/
    └── diabetes_tipo2.md
```

o Python poderia mostrar:

```text
Documentos carregados: 3

vacina_gripe.md
vacina_covid.md
diabetes_tipo2.md
```

Nesse ponto teremos:

```text
FONTES CONFIÁVEIS
        ↓
documentos selecionados
        ↓
data/
        ↓
Python lê os arquivos
        ↓
lista de documentos
```

Na **Etapa 8**, essa lista será transformada em **embeddings**, ou seja, representações vetoriais que permitirão buscar informações por significado e não apenas por palavras exatas.

Para concluir a **ETAPA 7**, eu colocaria estas metas:

```text
[ ] Criar pasta data/

[ ] Definir temas iniciais

[ ] Selecionar fontes confiáveis

[ ] Criar pelo menos 10 documentos

[ ] Cada documento possuir:
    - título
    - fonte
    - URL
    - tema
    - conteúdo

[ ] Python conseguir carregar todos os documentos
```

Aqui tem uma diferença importante para as etapas anteriores: **não vale inventar o conteúdo científico dos arquivos**. Os textos precisam vir das fontes confiáveis que vocês selecionarem.

Se quiser, podemos fazer essa etapa juntos começando por **vacinas**, montando os primeiros documentos reais da base e o código Python que vai carregá-los.

--------------------------------------------------------------------------------------------

Então fechamos essa parte com:

```text
Documentos encontrados: 18
Com conteúdo: 18
Vazios: 0
Estrutura válida: 18
Com problemas de estrutura: 0
```

Isso é um marco importante porque agora não temos simplesmente “18 arquivos”. Temos uma **base de conhecimento estruturada e validada pelo nosso próprio código**.

O fluxo já está assim:

```text
18 fontes de saúde (.md)
        ↓
6 categorias
        ↓
carregar_documentos.py
        ↓
verificação de arquivo vazio
        ↓
verificação dos campos obrigatórios
        ↓
18 documentos válidos ✅
```

### Onde estamos

```text
[X] 01 — Definir MVP
[X] 02 — Extensão Chrome
[X] 03 — Capturar resultados Google
[X] 04 — Cards visuais
[X] 05 — Backend FastAPI
[X] 06 — Extensão ↔ Backend
[X] 07 — Base de conhecimento ✅

[ ] 08 — EMBEDDINGS  ← PRÓXIMO
[ ] 09 — Busca semântica/vetorial
[ ] 10 — RAG
[ ] 11 — LLM local
[ ] 12 — Classificação
[ ] 13 — Evidências/fontes
[ ] 14 — UX de pensamento crítico
...
```

E agora começa uma parte **muito legal da IA**: **embeddings**.

Vamos pegar aqueles textos como:

```text
"A vacinação contra influenza é realizada anualmente..."
```

e transformar o significado deles em **vetores numéricos**:

```text
Texto
 ↓
Modelo de Embeddings
 ↓
[0.018, -0.273, 0.419, 0.052, ...]
```

Isso permitirá que depois alguém pesquise algo como:

> “tomar vacina da gripe todo ano é desnecessário”

e o sistema consiga encontrar um documento sobre **vacinação anual contra influenza**, mesmo que a frase pesquisada não seja idêntica ao texto do documento.

Esse é o fundamento da nossa futura **busca semântica e do RAG**.

**Próxima etapa: 08 — Embeddings.** Vamos fazer do mesmo jeito: uma coisa de cada vez, entendendo o que estamos fazendo antes de colocar código. 🚀


